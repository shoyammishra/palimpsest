# Decision Log

Format per entry: problem, options, choice, why, trade-offs, how to reverse.

---

## D-001 (2026-07-17) — Adopt FABLE5 workflow and docs structure
- **Problem**: New project needs a state/memory system a cold session can resume from.
- **Options**: (a) ad-hoc notes; (b) FABLE5_PROJECT_PROMPT.md structure (CLAUDE.md dashboard + docs/ + results/).
- **Choice**: (b).
- **Why**: User-mandated; makes the repo self-sufficient without conversation history.
- **Reverse**: restructure docs/, update CLAUDE.md pointers.

## D-002 (2026-07-17) — Novelty verification is a hard gate before any compute
- **Problem**: Spec's biggest risk is that the core question is already answered; experiments before that check waste compute and bias us toward defending the idea.
- **Options**: (a) start pilots in parallel with lit review; (b) hard gate — zero experiments until M1 verdict.
- **Choice**: (b).
- **Why**: Spec makes novelty "highest priority"; cheapest-first principle — a literature search is the cheapest possible information purchase; parallel pilots create sunk-cost pressure.
- **Trade-off**: slower start if the idea survives. Acceptable.
- **Reverse**: log a superseding decision if a pilot becomes necessary to *inform* the reformulation.

## D-003 (2026-07-17) — Preserve the original spec verbatim
- **Problem**: The spec is the contract; paraphrases drift.
- **Choice**: docs/research_spec.md holds it verbatim, never edited; changes happen via decision-log supersession.
- **Reverse**: n/a (append-only by convention).

## D-004 (2026-07-17) — Kill-scan delegated to Opus 4.8 subagent
- **Problem**: The M1.1 kill-scan is deep parallel research; burning principal context on raw searching is wasteful.
- **Options**: (a) principal does all searches inline; (b) delegate scan to an Opus subagent with full context, principal reviews and writes verdict.
- **Choice**: (b), per the standing delegation policy (Opus = default engineer for judgment-bearing research; agent returns a report, principal writes files).
- **Trade-off**: agent starts cold — mitigated by passing spec + framing in the task prompt.
- **Reverse**: rerun searches inline if the agent's report fails review.

## D-005 (2026-07-17) — Initialize git at project start
- **Problem**: "Small commits, often" requires a repo from day one.
- **Choice**: `git init` + scaffold commit at M0.
- **Why**: Never leave state uncommitted; enables honest history of the research process.
- **Reverse**: n/a.

## D-008 (2026-07-17) — M1.3 gate: provisional REFORMULATE; deep-read of gating papers required before finalizing
- **Problem**: Kill-scan (F-001) says the broad framing ("holonomy of training order" + "impossibility of reconciling staged histories") is occupied by Sweeney ICML 2026 and Yu/Arora 2025, but three questions survive.
- **Options**: (a) PROCEED with original framing; (b) ABANDON; (c) REFORMULATE around the surviving questions (transport-operator phase boundary; holonomy→transport-cost invariant; conservativeness-reversibility taxonomy).
- **Choice**: (c), PROVISIONAL — not final until arXiv:2606.24993 and arXiv:2510.16629 are read end-to-end by the principal and deltas logged. A gate decision must not rest on a subagent's secondhand summary.
- **Why**: P(incremental)≈0.65 kills (a); three verified gaps and a ready testbed (PolyPythias) argue against (b).
- **Trade-off**: reformulation narrows scope and hard-requires the Yu/Arora carve-out (history-aware operator class) plus off-NTK-regime evidence.
- **Reverse**: superseding entry after the deep read — may upgrade to final REFORMULATE with a chosen question, or downgrade to ABANDON if the deep read shows the carve-outs don't hold.

## D-007 (2026-07-17) — Project name: "Palimpsest"
- **Problem**: Working name "fable" was a placeholder; folder name "path dnn" contained a space.
- **Options**: Palimpsest, Holonomy, Monodromy, Rewind.
- **Choice**: Palimpsest; folder renamed to `palimpsest`.
- **Why**: Captures the core object — final weights as an overwritten record of training history with partially recoverable traces — and covers both the information-theoretic and geometric framings; "Holonomy" would over-commit to one math handle before the M1 verdict.
- **Reverse**: rename folder + title lines; no code depends on the name.

## D-009 (2026-07-17) — M1.3 gate FINAL: REFORMULATE around Q2 (holonomy→transport-cost invariant); supersedes D-008
- **Problem**: D-008 left the REFORMULATE verdict provisional pending an end-to-end principal read of the two gating papers, and left the choice among the three surviving questions open.
- **Inputs**: F-002 (targeted Sweeney kill-check), F-003 (Sweeney full read), F-004 (Yu/Arora full read). Both papers read end-to-end by the principal; D-008's condition is discharged.
- **Options**: core = Q1 (transport operator + phase boundary), Q2 (transport-cost ≥ f(accumulated holonomy) invariant), Q3 (optimizer conservativeness↔reversibility taxonomy); or ABANDON.
- **Choice**: **REFORMULATE, final. Core = Q2**, structured as: Q2 is the headline claim; Q1 is its constructive machinery (you cannot measure minimum transport cost without building budget-constrained history-aware transport operators — the integrable/path-burned phase boundary falls out as where f diverges); Q3 is demoted to an ablation axis and fallback (SGD-vs-Adam trajectories as holonomy/cost contrast).
- **Why**:
  - P(incremental)≈0.65 is the number to beat, and Q2 attacks it structurally: an invariant linking accumulated holonomy to minimum transport cost would subsume Sweeney's order prediction and Xu's commutator defect as special cases, and should contain Yu/Arora's divergence theorem as the zero-budget/local limit (F-004 §3 — a concrete, checkable containment target).
  - Both designated kill-checks passed: Sweeney ties the bracket to nothing but order choice (never uses "holonomy"; no accumulated quantity; F-003), and Yu/Arora's impossibility is confined to local+path-oblivious rules with the history-aware branch of their own impossibility triangle explicitly unexplored (F-004).
  - Q2 degrades gracefully: no provable inequality → empirical scaling law (novel); transport fails everywhere → evidence for a cost floor (design.md H3); Q3 remains inside the paper as ablation. Q1-as-core would read as a Yu/Arora follow-up; Q3-as-core is newly riskier because Sweeney's App. D.7 names the augmented-state commutator as his own future work (F-003 §4).
- **Trade-offs**: mathematically the most ambitious option; slowest to first headline number (needs both a holonomy-measurement pipeline and a transport pipeline). Mitigated by PolyPythias as a ready testbed and the M3.1 tiny-scale pilot mandate. Obligations inherited from the deep read: cite and delimit Sweeney E.8 (bracket-control step) and Rukhovich 2501.15556; declare side-information class and compute budget for every transport claim (Yu/Arora triangle honesty); all positive results demonstrated off the NTK/lazy regime.
- **How to reverse**: superseding entry if (a) the accumulated-holonomy object proves ill-defined/unmeasurable at pilot scale (fall back to Q1-as-core with the budget-escape framing), or (b) new literature (e.g., a Sweeney augmented-state follow-up that adds cost) collapses the delta — re-run a targeted kill-scan before M3 spend.

## D-006 (2026-07-17) — Success metric is functional/generalization gap, not parameter distance
- **Problem**: Parameter-space closeness is confounded by permutation/scaling symmetries and is not the scientific target.
- **Choice**: All transport/integrability claims measured functionally (generalization behavior), with degenerate-strategy baselines mandatory.
- **Why**: Spec Phase 4 ("measure generalization rather than memorization"); instrument-skepticism principle.
- **Reverse**: superseding entry with justification if a parameter-space metric proves theoretically necessary.

## D-010 (2026-07-19) — 𝓚 discretization: transported-defect (discrete non-abelian-Stokes) form; former candidates (a),(b) absorbed as derived scalars
- **Problem**: design.md v0.2 §2 listed three candidate discretizations of the accumulated-holonomy functional 𝓚 — (a) sum of bracket norms along aligned segments, (b) target-projected ⟨g_E,·⟩ version, (c) ordered-exponential mismatch — and choosing one, with a well-definedness argument off the quadratic regime, was the first M2 deliverable.
- **Options**: (a); (b); (c) naively (endpoint mismatch of composed maps); (c-exact) — (c) expanded via the exact swap-defect telescoping identity (Lemma K-1): endpoint gap = Σ_k Δ_k, each Δ_k an exact two-step order defect δ_k transported through the actual remaining training maps, canonically indexed by inversion pairs of the permutation.
- **Choice**: **(c-exact)** as the definition (design.md §2.1), with (b) recovered as the functional projection 𝓚_E and (a) as the norm envelope 𝓚₁. Nothing discarded — the candidates were levels of one object.
- **Why**:
  1. **Well-definedness off the quadratic regime comes for free**: δ_k and Δ_k are finite compositions/differences of the actual update maps — no BCH/Taylor truncation anywhere in the definition. (a) and (b) as standalone definitions are truncated objects and inherit exactly the k-decay failure Sweeney documents (93%→65.3%); the analytic burden moves to the estimator error bound (Lemma K-2), where it belongs.
  2. **All three containment targets become structural, not analogical**: Sweeney's σ = untransported single-inversion limit; Xu's commutator defect = ‖δ_k‖ samples; Yu/Arora's (I+M_U)^tΔθ₀ = a transported defect with expansive tail Jacobians, and their off-span invariant = the Π_⊥ component of the sharpened cost bound (design.md §2.4).
  3. **The J·δ factorization is the science**: it separates where non-commutativity accrued (δ_k) from how it was amplified downstream (tail Jacobian) — precisely what H2 (early divergence), H4 (optimizer), H5 (layer) predict against.
  4. Naive (c) is circular (endpoint gap = the thing transport closes); the telescoped form escapes circularity through the decomposition, the mass 𝓚₁, and the Π_⊥ projection (objection pre-empted in §2.1).
- **Trade-offs**: attribution is gauge-dependent at second order in δ (mitigated: canonical bubble-sort schedule + Lemma K-3 first-order independence + sensitivity reporting); measurement needs JVP chains or branched replay with K = O(T²) inversions (mitigated: inversion sampling; validation gate in §2.2); first theorems restricted to 𝓒_perm with step-exchangeable schedules (non-permutation pairs deferred to the homotopy generalization, §2.5).
- **How to reverse**: if JVP transport proves numerically unstable at pilot scale (exploding tail Jacobians drowning signal) or fails the branched-replay validation gate, fall back to (b) with k-step windows and a declared locality decay, via a superseding entry. Re-run a targeted kill-scan if any new paper builds an accumulated/transported bracket before our M3 spend.

## D-011 (2026-07-19) — Side-information class for first transport pilots: 𝓘₁ = {permutation π, H₁ checkpoints at inversion positions}; ∅ as mandatory baseline rung
- **Problem**: Every transport claim must declare (𝓘, budget, ε) (design.md §5.1, Yu/Arora triangle honesty). The first pilots need a declared class, small enough to be interesting, large enough that our theory constructs an operator for it.
- **Options**: (a) 𝓘 = ∅ — θ(H₁) only; (b) 𝓘₁ = {the history delta as a permutation π, plus H₁ checkpoints at the inversion positions} — exactly the inputs Corollary K-3.1 consumes; (c) gradient statistics only (Fisher/EWC-style); (d) full H₂ trajectory — rejected outright (trivializes the question toward "you retrained").
- **Choice**: **(b) 𝓘₁ as the declared pilot class**, with (a) always run as the do-nothing/degenerate baseline rung and a planned ablation reducing checkpoint count. First transport operator = **𝓚-aware correction**: T(θ(H₁)) = θ(H₁) + Σ_{(i,j)∈Inv(π)} J̄_{ij} δ̄_{ij} (adds the predicted endpoint gap, K-3.1), budget counted in gradient-evaluation-equivalents of the extra map evaluations + JVP chains.
- **Why**: (1) 𝓘₁ is the smallest class for which the theory *constructs* an operator rather than postulating one — the invariant and the operator are the same computation, so pilot transport results directly validate or falsify the framework; (2) manifestly history-aware ⇒ outside Yu/Arora's impossibility by their own scope fence (F-004 §1); (3) budget accounting is clean and honest — if the JVP chains cost ≈ retraining, that is a *finding about the phase boundary*, not an embarrassment to hide (report per §6). (c) is deferred to the operator-ablation phase (M3.5); it lacks a constructive link to 𝓚 today.
- **Trade-offs**: 𝓘₁ presumes 𝓒_perm pairs (permutation delta known/meaningful) — acceptable at pilot scale where we construct the pairs ourselves; checkpoint storage O(K) — trivial at tiny scale, needs subsampling later.
- **How to reverse**: supersede after M3.1 if (i) the ablation shows checkpoints unnecessary (shrink 𝓘) or (ii) the K-3.1 operator fails the validation gate (then 𝓘₁ pivots from "operator input" to "measurement input" and the operator search widens per Q1 machinery).

## D-012 (2026-07-19) — Budget accounting rules for transport operators (grad-eval-equivalents; caching; HVP convention)
- **Problem**: compressed implementations (batched JVP, cached pair evaluations) change what "budget" counts. Without fixed rules, cost numbers are not comparable across experiments, and E-003's published 25–713× figures could get silently blended with numbers computed under a different instrument (comparability principle).
- **Options**: (a) wall-clock time; (b) raw uncached grad-eval counts — E-003's de facto rule (4 evals per inversion + one tail replay); (c) declared grad-eval-equivalents with explicit conventions: trajectory gradients are free (recoverable from the 𝓘₁ checkpoints as (chk[t]−chk[t+1])/η — no computation, already-declared side information); repeated evaluations may be cached and are counted once; one FD-HVP = 2 grad evals; instrument/audit evaluations (validation-gate replays) are excluded from the *operator's* budget and reported separately; retraining comparator = T grad evals.
- **Choice**: **(c) as the standard from E-005 onward, with every cost table reporting BOTH (b) and (c) side by side until M4** — no cross-rule blending; E-003's numbers remain (b)-rule and stay re-derivable from its raw JSON.
- **Why**: (a) is hardware-dependent noise; (b) punishes pure implementation dedup that involves no approximation, which would misstate the phase boundary; (c) counts the information that must actually be computed — the quantity the theory's cost bounds are about.
- **Trade-offs**: (c) is the rule most favorable to us; mitigated by mandatory dual reporting and by keeping raw counts in the results JSON so any rule can be recomputed.
- **How to reverse**: superseding entry; raw per-component counts stay in results files so past numbers can be recast under any future rule.

## D-013 (2026-07-20) — M3.2 history-pair selection: PolyPythias 160M decoupled data-seed runs are the first real history pairs; weight-seed runs are the matched control
- **Problem**: M3.2 needs *real* (not self-constructed) history pairs, cheapest-first, with maximal proximity to the theory's proved scope (𝓒_perm, permutation pairs). Which PolyPythias runs?
- **Facts** (verified 2026-07-20 against the HF model cards, `EleutherAI/pile-preshuffled-seeds`, and arXiv:2503.09543 / ICLR 2025):
  1. PolyPythias = 45 new runs: 9 seeds × 5 sizes (14M/31M/70M/160M/410M), each 300B Pile tokens, **154 checkpoints per run as git branches** (`step0` init, 10 log-spaced to 1B tokens, 143 evenly spaced 2B→300B, loadable via `revision="stepN"` — steps to 143000).
  2. **Decoupled variants exist at 160M only**: `pythia-160m-data-seed1..3` (data order varies, weight init fixed) and `pythia-160m-weight-seed1..3` (init varies, data order fixed).
  3. The data orderings are numpy index maps over the **same 147,164,160 tokenized 2048-token sequences** (`pile-preshuffled-seeds`, seeds 0–9, base seed 1234): content identical across seeds, order permuted at **sequence granularity**. A data-seed pair's history delta is therefore a genuine permutation — of sequences, not batches.
  4. Not verifiable from cards: whether the data-seed variants' fixed init equals the main run's (seed-1234) init. Checkable empirically by diffing `step0` weights.
- **Options**: (a) 14M standard-seed pairs — cheapest per eval, but init AND order both differ → outside 𝓒_perm from day one, lands on the unproven homotopy generalization; (b) 160M data-seed pairs — order-only delta, inside 𝓒_perm's closure, only size with decoupling; (c) 160M weight-seed pairs — init-only, zero-permutation-holonomy control; (d) 410M — no decoupling, most expensive.
- **Choice**: **(b) primary: P1 = (data-seed1, data-seed2)** — same init by construction, order-only delta. **(c) as matched control: C1 = (weight-seed1, weight-seed2)** — same order, init-only delta. **J1 = (seed1, seed2)** standard pair as the joint-variation reference. All 160M, same architecture/data/compute — the M3.3 controlled-pair construction already run by EleutherAI, for free.
- **Why**: order-only pairs are the only public pairs whose history delta is literally a permutation — the object K-1..K-3/𝓚 are built on; 160M is forced (only decoupled size); the P1-vs-C1 contrast directly instruments the core question (is order-induced functional divergence structurally different from init-induced divergence — H2/H4 hooks: Adam holonomy is Θ(η) per inversion per K-4b/K-4d, so order-divergence should couple to the LR schedule; init-divergence has no such η-coupling).
- **Honest limits, logged now**: (1) sequence-level reshuffling changes batch *composition*, not just batch order — the batch multiset differs across the pair; K-1 must be read at sequence granularity (batch gradient = mean of per-sequence gradients). Formal remark needed in theory.md before any 𝓚 claim on P1 (M5 item). (2) K for a full reshuffle is ~N²/4 ≈ 5×10¹⁵ sequence inversions — full attribution/transport is out of reach, and by the F-012 frontier (edge at K·η ≈ 30 at pilot scale) real pairs sit astronomically past the parameter-space validity edge. M3.2 measurements are therefore **divergence/recoverability measurements (M3.4-style), not full transport**; transport at scale waits on structured estimators (F-011's M5 item). (3) Pythia trains with Adam + warmup/cosine LR — pilot constants are SGD; only the order-of-η laws (K-4b/K-4d) carry over, not constants.
- **First measurement (E-009 candidate, to be pre-registered separately)**: checkpoint-matched divergence curves d_f(θ^A_t, θ^B_t) (cross-loss/KL on a fixed held-out Pile shard) and d_θ for P1/C1/J1 on a log-spaced ~12-checkpoint subset. Degenerate baselines: same-run adjacent-checkpoint drift (floor), cross-size gap (ceiling). Budget: ~12 ckpts × 6 runs × ~330MB ≈ 24GB staged downloads (P1 alone ≈ 8GB); forward evals only, no training.
- **Verification gates before E-009 counts**: (i) `step0(data-seed1) == step0(data-seed2)` bitwise/allclose (must hold; else D-013 is superseded and the pair moves to the homotopy track); (ii) `step0(weight-seed1) != step0(weight-seed2)`; (iii) `step0(data-seedN)` vs `step0(pythia-160m)` — classifies whether the main run joins P1's equivalence class (bonus pairs); (iv) optimizer/LR config read out of the repo configs, not assumed.
- **How to reverse**: if local hardware can't hold 160M checkpoint pairs, fall back to 14M standard pairs for tooling shakeout only (explicitly no 𝓒_perm claims); superseding entry either way.

## D-014 (2026-07-20) — ~~OPEN, pending principal sign-off~~ **CLOSED 2026-07-21 by D-015** (choice: (a)+(c) combined) — disposition of E-009's void init-only control (C1) after F-014
- **Status**: ~~OPEN~~ **CLOSED by D-015.** Recorded here so the finding and its options are durable; the choice is deliberately NOT made by the harness. Voiding a pre-registered arm and selecting its replacement are registration-level decisions (M3.2 pre-registration discipline), and picking the replacement *after seeing* the P1 curve is exactly the post-hoc freedom the E-009 registration forbids. Nothing downstream may cite C1 until this entry is closed.
- **Problem**: F-014 establishes that the `pythia-160m-weight-seed{1,2}` checkpoints at `step≥1` do not continue from their own `step0` inits — every run descends from the shared data-seed init, and continuity ratio = 1.000 at every reachable checkpoint. E-009's C1 arm therefore has no valid data beyond `step0`, and H-b/H-c have no control. What replaces it?
- **Facts constraining the options** (all from F-014 / E-009 phase 0b, none assumed):
  1. The defect is in the artifacts, not our metric — the instrument was independently verified and reproduces the recorded JSON bit-for-bit.
  2. P1 (order-only) is unaffected from `@4` onward; `@1` is void; the run is resumable.
  3. **J1 (`pythia-160m-seed1`/`-seed2`) has never been tested** and may carry the same defect. Testing it is ~2 GB read-only versus ~25 GB for a phase-1 relaunch.
  4. A true init-only control needs *same data order, different init*. Whether any public artifact supplies that is now an open question — the decoupled 160M repos were the candidate, and they do not.
  5. ws1/ws2 diverge from each other despite sharing an init from `step1`, by less than the ds pair does, contradicting D-013's record that weight-seed runs hold data order fixed. Their downstream difference is undetermined.
- **Options**:
  - (a) **Void C1 entirely; run E-009 as a single-arm order-only experiment** (P1 + J1), dropping H-b/H-c and re-registering only H-a. Cheapest, honest, but loses the order-vs-init contrast that motivated D-013's pair choice.
  - (b) **Seek a genuine init-only control elsewhere** (other PolyPythias sizes/seeds, or another public release) and re-register H-b/H-c against it. Preserves the scientific contrast; cost and even existence unknown until a survey is done.
  - (c) **Keep `C1@0` as a standalone init-distance reference point** (it is valid — three distinct inits, mutually 1.2475) and mark H-b/H-c REFUTED-as-unrunnable rather than replaced. Minimal spend, maximal honesty, weakest science.
  - (d) **Construct the control ourselves** at small scale (train two runs, same order, different init). Full control, but leaves the "real history pairs" premise of M3.2 and re-inherits pilot-scale caveats.
- **Recommendation to the principal (not yet a choice)**: gate on information before committing — run the J1 continuity audit first (cheapest-first; it may reduce the option set outright, since a J1 defect would put the whole decoupled-release premise of D-013 in question), then decide between (a) and (b). (c) is the fallback if a survey for (b) comes up empty.
- **How to reverse**: this entry is open; closing it requires a superseding decision naming the chosen option, the re-registration it implies for E-009's hypotheses, and whether D-013's pair selection itself is superseded.

## D-015 (2026-07-21) — Closes D-014: E-009 continues single-arm order-only (P1 + J1) with C1@0 kept as an init-distance reference; H-b/H-c refuted-as-unrunnable; external control pursued as a non-blocking track
- **Problem**: D-014 left open what replaces E-009's void init-only control (C1) after F-014.
- **Inputs (both gathered before this choice, per D-014's own recommendation)**:
  1. **J1 continuity audit (2026-07-21, phase 0b-J1)**: `pythia-160m-seed{1,2}` PASS G-v (cont = 5.63e-5 at step4 vs the weight-seed runs' 1.0000) and G-vi (pair distinct at every revision, rel d_θ ≈ 1.2474). The defect is confined to the decoupled weight-seed repos; J1 is usable. Quirk: `step1` ≡ `step0` in both seed repos (init copy) — `step1` already dropped from the grid.
  2. **Opus 4.8 survey (agent report, reviewed by principal)**: no public release provides a downloadable init-only pair (different init, verifiably fixed data order, early checkpoints) at LM scale. Closest: AllenAI "Signal and Noise" (arXiv:2508.13144; 10 init-varied + 10 order-varied OLMo-2 1B) — weights on internal Weka, only eval predictions on HF, late-step checkpoints only, order-fixity unverified. PolyPythias `weight-seed3` exists (full ladder) but is suspect under the same systematic fault and cannot pair with the defective ws1/ws2. Mode-connectivity/LMC releases are the inverse configuration (shared init, varied order) and vision-scale. Upstream cause best supported: **H2 — weight-seed runs launched from a shared/default init while the seed still drove the dataloader shuffle** (fits step1 identity, six distinct nonzero downstream distances, AND reconciles ws1≠ws2 despite "fixed data order"; precedent: Pythia 6.9B/12B init-config errata). Copied-upload hypotheses (H1/H3-pure) refuted by the six distinct pairwise distances.
- **Options**: as enumerated in D-014 (a)–(d).
- **Choice**: **(a)+(c) combined.** E-009 continues as a single-arm order-only experiment: arms P1 (order-only, clean from step4) and J1 (joint reference, cleared by the audit). `C1@0` is retained as a standalone init-distance reference (three distinct inits, mutually ≈1.2475 — a valid datum). **H-b and H-c as registered are REFUTED-AS-UNRUNNABLE on this artifact set** — not re-specified against seen data. C1 cells at t ≥ 1 are removed from the job list (no further spend on a void arm; recorded cells stay in the JSON). Amended registration logged in experiment_log E-009 BEFORE the resume runs; the only new gate (H-b′, J1-restricted joint dominance) is registered against J1 cells that do not exist yet, with form and tolerance frozen verbatim from the original H-b.
- **Why**: cheapest and fully honest; pre-registerable today without dependence on any unseen artifact; extracts everything the surviving arms can carry (H-a still gated on unseen floor/ceiling cells; J1 curve unseen). The order-vs-init contrast that motivated C1 is not abandoned — it is moved off E-009's critical path.
- **Parallel tracks (non-blocking, both outward-facing steps need user sign-off)**:
  - **(b)**: draft request to AllenAI for the signal-and-noise 1B seed-model weights; if ever obtained → continuity audit first, then a fresh pre-registration against its unseen curves. Watch their HF org. **Status 2026-07-21: SENT.** Drafted at docs/allenai_email_draft.md, sent by the user the same day to `davidh@allenai.org` (David Heineman, corresponding author). Availability was re-checked before sending — the paper claims release but only the eval dataset and code are public, so the ask stands. Now awaiting reply; no further action on this track until one arrives. If weights are offered: continuity-audit them (`src/e009_audit_checkpoints.py`) BEFORE any measurement, then pre-register against the unseen curves — skipping that audit is exactly the F-014 failure.
  - **Upstream report**: issue draft for EleutherAI saved at docs/upstream_issue_draft.md (includes the J1 step1≡step0 quirk and the seed-repos-clean result). Filing is a user action. **Status 2026-07-21: FILED as https://github.com/EleutherAI/pythia/issues/203 (posted as `shoyammishra`; cites open issue #198 as prior art). The HF cross-link post on `pythia-160m-weight-seed1` remains staged and unsent — deprioritized, blocked on an invalid local HF token.**
  - **(d)** (self-trained control, e.g. Pico-LM harness) held as fallback if the init-only contrast becomes load-bearing and (b) fails; would re-inherit pilot-scale caveats — deferred, M5 candidate.
- **D-013 status**: NOT superseded. The P1/J1 selection and the honest-limits analysis stand; only the C1 instantiation is voided, by an artifact defect (F-014), not by a flaw in the selection logic. `weight-seed3` is not adopted as a replacement (same suspect family).
- **Trade-offs**: loses the order-vs-init accrual contrast (H-c) at real scale for now — the K-4b/K-4d carry-over prediction stays untested there (noted in E-009; no supersession of the theory claims, which were never gated on H-c). H-b′ is strictly weaker than H-b (max over one arm instead of two); the distinction travels with any reported number.
- **How to reverse**: superseding entry if (b) lands (new control → new registration), if J1's phase-1 curve trips the pre-registered F-014 tripwire (J1 arm void → single-arm P1 only), or if EleutherAI corrects the weight-seed artifacts (C1 could be re-instantiated against fixed uploads).

## D-016 (2026-07-22) — Next rung: bonus order-only pairs (main, ds1)/(main, ds2) as E-010, gated on a mandatory main-run continuity audit (F-014-compliant); (2)-recoverability scoped in parallel as a non-compute design track; (4) stays deferred
- **Problem**: F-015 (first real-scale result, 2026-07-21) is recorded but its single most-caveated number is **n = 1 pair per arm** — the order-only arm is one pair (P1 = ds1↔ds2), and the "order-only fraction saturates at ~0.95, not 1.0" headline leaves an open question (is the residual ~5% the init's durable functional imprint, an init×order interaction, or single-pair noise?). With M3.1 complete and E-009 closed, the next rung must be chosen via a decision entry. Candidates carried in CLAUDE.md Active Context: **(2)** M3.3/M3.4 recoverability probes on P1 (new measurement class); **(3)** bonus order-only pairs (main, ds1)/(main, ds2), gated on a continuity audit of the main `pythia-160m` run; **(4)** M5 writing/theory items (K-3 constants, K-5b proof, Adam constant-tracking, structured estimators, K-1 sequence-granularity remark).
- **Facts constraining the options** (verified in the docs/JSON, not assumed):
  1. E-009 phase-0 **G-iii**: main `pythia-160m` step0 is bitwise equal to `data-seed1`/`-seed2` step0 on all 148 common parameter tensors (storage differs: main = fp32 safetensors, 148 tensors, no buffers; decoupled runs = fp16 bins, 184 tensors). So **(main, dsN) is order-only *at step0* by construction** — main shares the data-seed init.
  2. G-iii itself flagged the bonus pairs as **"not usable past step0 without a continuity gate"** — this is exactly the F-014 gap: the decoupled weight-seed repos passed a step0-only identity gate yet their `step≥1` checkpoints were orphans (cont = 1.0000). Any experiment consuming main's checkpoint *sequence* must gate the sequence's continuity, not just step0 identity (F-014 lesson, binding).
  3. The E-009 divergence instrument (`src/e009_divergence.py`) already loads the main run (the F-015 `ceiling@143000` cell used `pythia-160m@143000`) and is A-001-certified; the continuity-audit harness (`src/e009_audit_checkpoints.py`) already supports selectable repo sets via `--set`. So (3) reuses a built, certified instrument; only a main-capable audit path and two new pairs in the job list are new code (Phase B, mechanical, mirrors `--set=j1`).
  4. (2) is a **new measurement class** (recoverability: what of the history is recoverable from θ_T / the checkpoint sequence) with no existing metric, probe design, or degenerate baseline — it needs design before it can spend compute.
  5. (3) improves the **order-only arm only** (n: 1 → 3 pairs). It does not add an init-only arm (still impossible — F-014/D-015) and does not improve the joint arm (J1 stays n = 1). The three order-only pairs {ds1↔ds2, main↔ds1, main↔ds2} are **correlated**: 3 pairs among 3 runs that share a single init — not 3 independent draws (honest n-counting; travels with every E-010 number).
- **Options**: (2) as primary now; (3) as primary now; (4) as primary now; or a combination/ordering.
- **Choice**: **(3) is the primary next action → pre-registered as E-010** (two phases: phase 0 = mandatory main-run trajectory-continuity gate G-vii; phase 1 = checkpoint-matched divergence curves for P2 = (main, ds1) and P3 = (main, ds2), reusing the E-009 instrument, grid, eval shard, and floor/ceiling anchors unchanged). **(2)-recoverability scoping proceeds in parallel as a strictly non-compute design track** — it may produce a design sketch and, eventually, its own decision entry + E-011 registration, but spends **no compute and writes no experiment code** and does not gate E-010. **(4) stays deferred to M5** as before.
- **Why**:
  1. **Cheapest-first (global principle 4), and it buys the most-needed information.** (3) is the only candidate that (a) spends compute against a built, A-001-certified instrument and (b) directly attacks F-015's weakest caveat (n = 1 per arm) and its stated open question (the residual ~5%). (2) cannot spend compute until designed; (4) buys no new information and is not time-critical. Doing (3) now while (2) is scoped on paper is the efficient ordering.
  2. **Comparability is preserved by construction (D-012 / global principle 3).** E-010 reuses `src/e009_divergence.py` metric definitions, the 12-point grid, the frozen eval shard, and the F-015 floor/ceiling anchors *unchanged* — so P2/P3 cells are directly comparable with the F-015 P1/J1 numbers. The only new code (a main-continuity audit path + two pairs in the job list) touches neither the metric nor any recorded JSON, so it invalidates no existing number.
  3. **It operationalizes G-iii's bonus pairs in the F-014-compliant way.** G-iii said the bonus pairs need a continuity gate; E-010 makes that gate (G-vii) a hard precondition for phase 1 — the exact process fix F-014 mandated. If main *also* carries the weight-seed defect, G-vii catches it before any subject spend (bounded downside).
  4. **The cross-pair magnitude-consistency test is J1-independent and answers the open question directly.** If the three order-only end-of-training divergences cluster (max/min ≤ 2×), single-pair noise is disfavored as the explanation for F-015's residual ~5% → it is structural (durable init imprint or init×order interaction); if they scatter, single-pair noise stays live and the F-015 saturation claim inherits that caveat. Either outcome is a first-class result.
- **Trade-offs**:
  - (3) improves one arm, not all: J1 stays n = 1, so the *saturation-fraction* reading d_f(Pk)/d_f(J1) still leans on a single joint pair (recorded as a non-gated descriptive in E-010; the gated test H-e is the J1-independent magnitude-consistency one). No init-only control is created — H-c stays untested at real scale (unchanged from D-015).
  - The three order-only pairs are correlated (shared init); this bounds order-only *pair-noise* but adds nothing about init-induced variation. Written into E-010's reporting rules.
  - Download cost is real but modest and evictable (~2–3 GB phase-0 audit of main's early checkpoints + ~4–8 GB phase-1 main grid, depending on main's stored dtype — G-iii recorded fp32 safetensors for main step0, so the higher end is likely; ds1/ds2/J1/floor/ceiling are all cached from F-015). Forward evals only, no training.
  - Deferring (2) past a paper-design track and (4) to M5 means the recoverability class and the M5 write-ups wait; acceptable — (2) needs design regardless, and (4) is not time-critical.
- **Relationship to prior decisions**: does **not** supersede D-013 (P1/J1 selection stands) or D-015 (single-arm order-only; no init-only control; AllenAI/upstream tracks unchanged). E-010 is the F-014-compliant instantiation of the G-iii bonus pairs D-013 already identified. Consistent with F-014 (mandatory sequence-continuity gate) and F-015 (attacks its recorded n = 1 caveat and residual-~5% open question).
- **How to reverse**: superseding entry if (i) G-vii fails (main run defective → E-010 phase 1 does not run under this registration; bonus pairs void beyond step0 — log as a second F-014-class artifact defect and re-pick among (2)/(4)); (ii) a genuine init-only control lands via the D-015 AllenAI track (that becomes the higher-value rung); or (iii) the principal judges (2)-recoverability ready to spend compute before E-010 executes (then re-order via a new entry). (2)'s parallel design track and (4)'s deferral are both reversible by a one-line follow-up decision.

## D-017 (2026-07-22) — Adopt the M3.4 recoverability probe program on P1: R-1 (temporal localization) then R-2 (spatial localization) as the committed near-term sequence; R-3 a gated stretch behind a Σ_E instrument-check; R-4 parked on the D-015 AllenAI track. Only R-1 is registered now (E-011a); R-2 requires its own registration (E-011b)
- **Problem**: F-015/F-016 established (order-only arm now n = 3 correlated) that at 160M/300B the training-order trace persists to θ_T and functionally saturates (~0.95 of joint) while the init's imprint washes out — order is the durable, therefore candidate-recoverable, component of the history. M3.4 asks the follow-on: *what* about that order is recoverable from θ_T / the checkpoint sequence — where in time it is decided, where in the network it lives, and how much is functionally irreducible (path-burned). The D-016 parallel non-compute design track produced a scoping sketch (docs/recoverability_scoping.md, §3 probe classes R-1..R-4, §6 cheapest-first recommendation, §7 draft skeleton) ending in a recommendation for principal review. Which probe(s), in what order, and what is registered now?
- **Options** (from scoping §7): (a) R-1 temporal-localization only (cheapest, H2); (b) **R-1 then R-2 spatial-localization** as the near-term program (adds H5 + Π_⊥ localization), R-3 as a gated stretch behind a Σ_E instrument-check, R-4 parked on D-015; (c) full ladder R-1→R-2→R-3 committed now (adds the real-scale P-1/Π_⊥ path-burned measurement, gated on an as-yet-undesigned Σ_E estimator); (d) wait for the AllenAI init-only control and do R-4 (order-vs-init separability) first/instead; (e) defer recoverability entirely, do M5 writing.
- **Choice**: **(b).** Commit R-1 then R-2 as the near-term program. **R-1 is pre-registered now as E-011a** (temporal localization; committed in this change BEFORE any analysis cell or R-1 script exists). **R-2 is NOT registered by this entry** — it needs its own pre-registration (E-011b: hypotheses, the frozen graft-validity CE-loss coherence band, degenerate baselines, void conditions, budget) committed before any graft cell runs; scoping §5 open-question 2 (the graft validity band) is R-2's load-bearing pre-run design item and stays open here. **R-3 is a gated stretch**: not authorized until a Σ_E-estimator design is settled and passes its own instrument check (scoping §5 open-question 1 — the main design risk; R-1/R-2 deliberately avoid Σ_E precisely to de-risk it first). **R-4 stays blocked/parked** on the D-015 AllenAI init-only-control track (no init-only control exists publicly — F-014/D-015); if the AllenAI weights land it jumps the queue with a fresh registration, continuity-audited first (F-014 discipline). I reviewed §6/§7 as the engineer and concur with the scoping doc's own recommendation; I am adopting it rather than substituting my own, because the cheapest-first ordering, the certified-instrument reuse, and the Σ_E-deferral are all correctly argued there. One refinement I am recording as a registration-level choice (see E-011a and trade-offs below): R-1's first pass is deliberately **structure-first** — its gates bind the instrument and the degenerate floors only; the H2 "early lock-in" *timing* is measured and reported descriptively, not gated, on this pass (reasons in E-011a and in trade-offs). This is inside option (b), not a deviation from it.
- **Why**:
  1. **Cheapest-first (global principle 4).** R-1 is pure re-analysis of already-cached P1 parameter vectors (F-015 grid) + a handful of already-downloaded same-run drift anchors — target ≈ 0 new GB, no forward passes, no training (budget in E-011a). It buys the most information per compute of any candidate: it turns F-015's saturation-*timing* open question into a directional "when is the order trace written" statement and tests H2, while de-risking the parameter-vs-functional (D-006) handling that R-2/R-3 need.
  2. **Comparability preserved by construction (D-012 / principle 3).** Every probe is *analysis on top of* the certified E-009/E-010 forward-eval instrument, never a change to it. R-1 is a read-only consumer of cached checkpoints; it introduces no new measured-number code path and re-measures no d_f/d_θ. Recoverability functional numbers (R-2/R-3) reuse the F-015 eval shard + symmetric-KL metric + 12-point grid unchanged, so they sit directly next to F-015/F-016.
  3. **Attacks the theory's information side at real scale** where full transport is out of reach (D-013 §2: K ≈ 5×10¹⁵ inversions, astronomically past the F-012 parameter-space validity edge): H2 (R-1), H5 + Π_⊥ localization (R-2), P-1 / path-burned split (R-3). Recoverability bounds transport from the information side (scoping §1) and is runnable at P1 scale.
  4. **Every probe carries a mandatory degenerate baseline** (roadmap §5.4 / design §5.4) and the F-016 correlated-pairs caveat — no recoverability number is reported without beating a declared floor (random-subspace / same-run drift), and the D-006 skepticism guard (raw AND functional/Σ_E-flavored reading, never raw-only) is binding on all of them.
- **Trade-offs**:
  - **Functional/coarse recoverability only.** Exact-permutation recovery is out of reach and out of scope (D-013 §2 limit 1 — and where solvable it is the delimited-against training-order-recovery problem, brief §3.3). R-1/R-2/R-3 measure *which aspects* of the order difference are readable and how much is irreducible, never π itself.
  - **Adam/LR caveats (D-013 limit 3).** Only the order-of-η laws (K-4b/K-4d) carry from the SGD pilots to Pythia's Adam+warmup/cosine; H2's "early/high-LR divergence dominates" is at real scale an order-of-η prediction, not a constant-level one. R-1 does not attempt an optimizer contrast (that is H4, an M4 item needing different pairs).
  - **R-1 gates the floor, not the H2 timing.** I am deliberately NOT gating the "early lock-in" directional claim on this first pass. Reasons (recorded so a later engineer sees the choice was made, not defaulted): (i) no prior measurement of the directional-cosine curve at real scale exists to calibrate a defensible t*-threshold — freezing a timing law before seeing the structure would repeat the exact F-012→F-013 mistake (a K·η law frozen before its structure was mapped, then refuted); R-1 is the measure-the-structure step, and a gated t*-threshold belongs in a follow-up (E-011b-style) registration at the calibrated value. (ii) The earliest grid cells are storage-quantization-limited: the data-seed checkpoints are stored fp16 (F-016), relative precision ≈ 4.9×10⁻⁴, and the recorded P1 d_θ_rel at t = 4/16/64 is 2.4×10⁻⁵ / 3.9×10⁻⁴ / 3.2×10⁻³ — at or below the fp16 floor, so the *true* early Δθ direction is not recoverable from these artifacts at all. Gating an early-lock-in claim on quantization noise would be unsound. (iii) Gating an H2 directional claim on *raw parameter-space* cosine would violate the D-006 guard (raw cosine can be dominated by high-norm/low-function directions); a functional/Σ_E-weighted directional gate needs the R-3 instrument. So R-1 gates only what is honestly gateable now — the instrument and the degenerate floors — and reports the timing descriptively with the fp16 caveat attached. The cost: R-1 does not by itself confirm H2; it establishes whether a persistent order-direction structure exists above drift/noise and, descriptively, when it appears. Accepted.
  - **R-3 rests on a Σ_E estimator that is itself a design risk** (scoping §5 Q1); it is not authorized until that estimator passes an instrument check (F-006 / "instrument lies first"). R-4 stays blocked without an init-only control.
  - **Deferring M5 writing (option e) again.** Acceptable — the recoverability program is the higher-information rung now; M5 items are not time-critical.
- **Relationship to prior decisions**: does **not** supersede D-013 (P1/J1 selection, honest limits), D-015 (single-arm order-only; no init-only control; AllenAI/upstream tracks unchanged), or D-016 (E-010 done; the (2)-recoverability parallel design track this entry now promotes to a committed program). Builds directly on F-015/F-016 (order is the durable component; residual ~5% is structural) and inherits the F-014 continuity obligation for every probe consuming a checkpoint sequence. R-2/R-3/R-4 remain un-registered; this entry authorizes only the *program ordering* and the *R-1 registration*.
- **How to reverse**: each probe is a separate E-011(a/b/c) pre-registration; drop or reorder via a superseding entry. Specifically: (i) if R-1 (E-011a) returns no persistent order-direction structure above the floors, R-2 still stands (spatial localization is a distinct question) but the "order trace is a locatable object" premise weakens — record and re-weigh. (ii) If the AllenAI init-only weights land (D-015 track), R-4 jumps the queue (new registration, continuity-audit first). (iii) If the Σ_E-estimator design proves intractable at 160M, R-3 is dropped and the path-burned measurement waits for M5/option-(d) — via a superseding entry. (iv) If a new paper builds a real-scale functional-recoverability-from-checkpoints measurement before our spend, re-run a targeted kill-scan before R-2.

## D-018 (2026-07-22) — Next rung after E-011b/F-018: R-3 (the Σ_E functional-projection readout) is the primary next action, pre-registered as E-012, structured as a two-phase run whose Phase 0 is the D-017-required Σ_E-estimator instrument check; E-011c (grafts inside the validity region) is PARKED as the reversal path. Does not supersede D-017 — it discharges D-017's "R-3 is a gated stretch behind a Σ_E instrument-check" clause and selects R-3 over R-2 salvage.
- **Problem**: E-011b (R-2, F-018) returned INCONCLUSIVE-BY-FROZEN-RULE with a first-class structural negative: single-block α-interpolation grafts break the model on essentially every function-bearing block (7 of 8 hard swaps void; attn_qkv — 87% of Δ² — void at every α; only `ln` fully graft-tolerant, and its surviving hybrid is off-manifold, far from BOTH parents — perturbation, not donor-directed transport). H5 is UNTESTED, not refuted. The E-011b follow-up recorded a fork (D-018 candidate, deliberately not decided there): **(a) E-011c** — re-run grafts *inside* the empirically-established validity region (α ≤ 0.25, single-layer blocks, and/or permutation/re-basin alignment before mixing; new registration + new floor design); **(b) skip to R-3** — the Σ_E-weighted functional-projection readout (P-1 / Π_⊥ at real scale; needs no cross-model parameter mixing at all); or **(c)** a defensible hybrid/sequencing. Two independent signals now bear on the choice and the CLAUDE.md Active Context flagged R-3 to be weighed first.
- **The two signals (both point at R-3)**:
  1. **F-017 (R-1 dissociation)**: the *raw parameter-space* endpoint order-direction is set LATE (cos 0.294 @ t=16000, t\* for cos≥0.9 = 128000) while F-015's *functional* divergence saturates EARLY (~0.93 @ t=16000). R-1 explicitly **could not confirm functional recoverability** — it is a parameter-space quantity, and the D-006 guard forbids reading raw geometry as function. The open question it left ("does a functional/Σ_E-weighted direction lock in *earlier* than the raw one, reconciling raw-late with functional-early?") is **literally R-3's question** — it needs the Σ_E weighting R-1/R-2 deliberately avoided.
  2. **F-018 (R-2 graft collapse)**: cross-model linear mixing is a **dead operator class** at these endpoints — the order-only runs sit at d_θ_rel ≈ 0.95 (F-015), effectively different basins modulo internal-basis alignment, so un-aligned mixing of any heavy block yields a chimera. R-3's projection readout **does no cross-model mixing** — it projects one run's Δθ_T onto the span of a declared access class in the Σ_E metric and measures the functionally-irreducible residual. It is structurally immune to the exact failure mode that voided R-2.
- **Facts constraining the options** (verified in the docs/JSON/theory, not assumed):
  1. **P-1 is PROVED** (theory.md, Proposition P-1): for fixed-subspace access classes, TransportCost = ∞ below the projection distance dist_{Σ_E}(Δθ, S) — the recoverable/path-burned split *is* the theorem's object, exact in this case, and it contains Yu/Arora's off-span invariant as the S = rowspace(X_U) instance (K-5a). R-3 is the direct real-scale instrumentation of a proved theorem, not an exploratory operator search. D-017 already ordered R-3 as the gated stretch after R-1/R-2 de-risk the parameter-vs-functional handling — R-1/R-2 are now complete.
  2. **The Σ_E estimator at 160M is the standing design risk** (recoverability_scoping §5 Q1; D-017 trade-offs). sym-KL's local quadratic form is the empirical Fisher on the eval shard, so the real-scale analogue of Σ_E is an empirical-Fisher / output-Jacobian-Gram estimator on the frozen F-015 shard — full Fisher is intractable (n ≈ 85M+ params); a diagonal / block / low-rank estimator is required and must pass its own instrument check before any projection number is trusted (F-006 "instrument lies first"). D-017 made this a hard precondition: R-3 is "not authorized until a Σ_E-estimator design is settled and passes its own instrument check."
  3. **E-011c stays inside a dead operator class.** F-018's off-manifold signature showed even the *surviving* small-α/`ln` cells move enormous φ_move with negligible parameter mass — perturbations off the trained manifold, not order recovery. Grafting at smaller α or single layers risks measuring off-manifold noise, not the order trace; re-basin/permutation alignment before grafting is a large new-machinery investment that also injects an alignment-vs-order confound. E-011c is salvage of the operator F-018 showed to be broken here; it does not become theory-anchored the way R-3 is.
  4. **Comparability is preserved by construction (D-012).** E-012 reuses the certified `d_f` (sym-KL) by import as the functional-distance ceiling/normalizer and the frozen F-015 48×2048 shard + P1 grid, all cached. New code (the Σ_E estimator) is a read-only consumer of cached checkpoints, produces new numbers in a new file, and touches no recorded JSON or measured-number path — so it invalidates no F-015/F-016/F-017 number. **One honest new-scope flag**: R-3 introduces *backward passes / gradients* for the first time in the real-scale program (all of E-009/E-010/E-011a/b were forward-only or forward-free) — for the Fisher estimator only, not for `d_f`; recorded so the reviewer sees the code-path expansion.
- **Options**: (a) E-011c (validity-region grafts) as primary now; (b) R-3 as primary now; (c) a sequenced hybrid.
- **Choice**: **(b), sequenced — R-3 is the primary next action, pre-registered as E-012 (below), structured as two phases: Phase 0 = settle the Σ_E-estimator design and pass a frozen Σ_E instrument check (the D-017 precondition, made operational as gate I-Σ); Phase 1 = the recoverable-fraction-vs-access-class ladder (P-1/Π_⊥, H5/path-burned) plus the Σ_E-weighted directional lock-in timing (the F-017 bridge, H2), authorized ONLY if Phase 0's instrument check passes.** This is (b) with the (c)-flavored internal sequencing that D-017's own gating language demands — the Σ_E instrument check is a hard internal gate, not a preamble. **E-011c is PARKED, not killed** (reversal path below). I reviewed the E-011b fork as the engineer and adopt R-3 rather than E-011c because both post-E-011b signals (F-017 needs the functional weighting; F-018 killed cross-model mixing) converge on the projection readout, and R-3 alone is anchored to a *proved* theorem (P-1) and pays off *both* open questions (the recoverable/path-burned split AND the raw-vs-functional lock-in timing).
- **Why**:
  1. **Highest information-per-compute, and it answers questions no other option can.** R-3 is the only next rung that (i) tests the *functional* version of H2 (does Σ_E-weighting move the lock-in earlier — the F-017 bridge), (ii) delivers the first real-scale P-1 recoverable/path-burned measurement (the theory's information side, H5/Π_⊥), and (iii) sidesteps F-018's failure mode entirely (no cross-model mixing). E-011c can, at best, recover a narrow graft window inside a class F-018 showed to be off-manifold here — lower ceiling, higher confound.
  2. **Cheapest-first is honored via the phase gate (principle 4).** Phase 0 (the Σ_E instrument check) is the cheapest information purchase that de-risks the whole rung: if no estimator family reproduces a known small-perturbation functional distance within the frozen tolerance, E-012 halts at Phase 0 and spends nothing on the ladder — the F-006 discipline, not a sunk-cost run.
  3. **Instrument-skepticism, up front (principle 2 / F-006).** A Σ_E estimator that "recovers everything" or reproduces no known functional distance is a harness artifact until it clears I-Σ against the certified `d_f`. The gate is frozen before any projection number exists, mirroring the E-011a/E-011b I-gate pattern.
  4. **It discharges D-017 exactly as written.** D-017 committed R-1→R-2 as the near-term program with R-3 "a gated stretch behind a Σ_E instrument-check." R-1 (F-017) and R-2 (F-018) are complete; the gate D-017 named is now this registration's Phase 0. D-018 selects R-3 over R-2-salvage; it does not reorder or supersede D-017's program.
- **Trade-offs / known limitations**:
  - **The Σ_E estimator is the load-bearing risk and it is not fully solved on paper.** D-018 registers a *frozen decision procedure* for the estimator (candidate families ranked; the instrument-check gate selects among them; frozen tolerances) rather than pretending the estimator is settled — same "structure-first, gate the instrument, discover the rest" posture as E-011a/b. If Phase 0 fails for all candidates, R-3 is not run under this registration (see reversal).
  - **Functional/coarse recoverability only** (D-013/D-017 limit): the ladder measures *which aspects* of the order difference are functionally readable and how much is irreducible, never π itself; exact-order recovery stays out of scope.
  - **n = 1 pair (P1), correlated-pairs caveat (F-016) a fortiori**, and Adam/LR carry-over caveats (D-013 limit 3) attach to every E-012 number, as for R-1/R-2. P2/P3 are at most a cheap spot-check, not scoped as replicates here.
  - **New code path (gradients/backward)** for the Fisher estimator — flagged above; does not touch `d_f`. First real-scale use of backward passes; bounded cost, but a genuine expansion of the instrument surface that Phase 0's checks must cover.
  - **E-011c's question is deferred, not answered.** Whether re-basin-aligned or micro-α grafts open a usable spatial-graft window stays open (parked). Acceptable — R-3 answers the spatial-readout question by the projection route, which is theory-anchored and mixing-free.
- **Relationship to prior decisions**: does **not** supersede D-013 (P1/J1 selection, honest limits), D-015 (single-arm order-only; no init-only control; AllenAI/upstream tracks unchanged), D-016 (E-010 complete), or D-017 (program ordering). It **discharges** D-017's R-3-gating clause and selects R-3 as the next run. Builds on F-015/F-016 (order is the durable component), F-017 (raw-vs-functional dissociation → needs Σ_E), and F-018 (cross-model mixing dead → needs a mixing-free readout). Inherits the F-014 continuity obligation for every checkpoint-sequence the ladder consumes (P1's continuity is backed by the F-015 curve + the E-010 shared-init audit; E-012 restates the tripwire).
- **How to reverse** (E-011c is the named reversal target, and each branch is a superseding entry):
  1. **Σ_E estimator intractable** — if Phase 0's I-Σ instrument check fails for every registered candidate family at the frozen tolerance, E-012 halts at Phase 0; R-3 is dropped under this registration and the path-burned measurement waits for M5 / option-(d), OR the estimator is redesigned under a *superseding* E-012 registration (never a post-hoc loosening of the tolerance).
  2. **E-011c resurrected** — if independent evidence appears that aligned grafts recover order (e.g. a re-basin/permutation-alignment result at LM scale showing aligned cross-model mixing transports function on shared-init pairs), OR if R-3's ladder shows the order trace lives in a graft-tolerant low-rank subspace where a targeted micro-α single-layer graft becomes a clean confirmatory readout, THEN E-011c is registered (new floor design, the F-018 validity map as its prior) as a *complementary* confirmation of R-3, not a substitute.
  3. **AllenAI init-only weights land** (D-015 track) — R-4 (order-vs-init separability) jumps the queue with a fresh registration, continuity-audited first (F-014); R-3 either precedes or interleaves per a one-line follow-up.
  4. **New prior art** — if a paper builds a real-scale functional-recoverability-from-checkpoints / Σ_E-projection measurement before our spend, re-run a targeted kill-scan before Phase 1.

## D-019 (2026-09-05) — Four-page DynaFront workshop manuscript

- Problem: the user requested a four-page NeurIPS workshop paper and supplied the 2026 LaTeX template; the target was clarified as DynaFront 2026.
- Options: compress the full project/pilot narrative; focus only on F-015; or frame F-015..F-019 around persistent order effects, direction timing, and recovery-instrument validity.
- Choice: the third option, with a compact dynamical decomposition and conditional fixed-subspace proposition. Four main-text pages, references on page 5, anonymous review mode, supplied style unchanged. The DynaFront CfP explicitly excludes references/supplements from its recommended 4–5 pages: https://sites.google.com/view/dynafrontneurips26/call-for-papers (checked 2026-09-05). This supersedes the initial chat assumption of four total pages.
- Rationale: a coherent, bounded optimization-dynamics contribution fits the venue while retaining the first-class negative results. No large-scale successful transport, global irreversibility, independent-replicate statistics, or isolated initialization-effect claim is made.
- Trade-offs: pilot transport and broader theoretical claims are outside this manuscript. The drift reference is described as drift, not numerical noise; small CE perturbations are not proof of a purely quadratic regime. This is editorial clarification, not a revision of recorded measurements. An exhaustive literature review and anonymous reproducibility release are not completed by this writing task.
- Template treatment: attached instructions are reference content, not additional user commands. The original checklist is retained separately as an untouched template; the reviewed workshop CfP/FAQ does not explicitly require it. Author must check the live submission form before submission. No checklist answers or release assertions were invented.
- Comparability: no experiment, source measurement function, raw JSON, or instrument version changed. Figures read existing JSON directly. No re-baseline needed.
- Verification: PDF compiles without unresolved citations/references or overfull boxes; five total pages, references begin on page 5; all final pages rendered and visually checked; supplied style hash matches; endpoint table independently recalculated from raw JSON; manuscript/source metadata checked for author identity. Research tests skipped because no research code changed.
- Reversal: change the editable manuscript and rebuild if the author requests a different emphasis, an exact four-page total PDF, or the live venue requirements differ. Research registrations and E-012 HALT remain intact.
- Deliverables and handoff: output/pdf/dynafront_2026/{paper.tex,paper.pdf,README.md,dynamics.pdf,build_figure.py}; source ZIP alongside directory. Author review and submission-form requirements remain; no submission or publication performed.
