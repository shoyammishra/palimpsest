# Experiment Log

M1 novelty gate passed 2026-07-17 (D-009, findings F-001..F-004) — compute spend unblocked for the M3 cheapest-first ladder. Rule: hypothesis logged BEFORE the run; one variable at a time; results appended, never edited into the hypothesis; instrument checks precede subject measurements.

---

## Entry template (copy for each experiment; fill hypothesis BEFORE running)

### E-XXX (date) — <one-line name>
- **Hypothesis**:
- **Variable changed** (exactly one):
- **Held constant**:
- **Config**: (model, data, optimizer, seeds, commit hash)
- **Degenerate-strategy baseline & expected score**:
- **Result**:
- **Verdict** (confirmed / refuted / inconclusive — and why):
- **Follow-up**:

---

### E-000 (2026-07-19) — Instrument self-checks (must pass before E-001/E-002 count)
- **Hypothesis**: the harness (`src/pilot_defects.py`, numpy-only, manual-backprop tanh MLP, float64) is correct: (1) manual backprop matches central finite differences to rel. error < 1e-6; (2) the measured SGD pair defect matches the *independently computed* leading-order prediction η²(H_a g_b − H_b g_a) (finite-difference HVPs, different code path) to rel. error < 5% at η = 1e-3; (3) K-1 telescoping: Σ_k Δ_k over a multi-swap schedule (branched replay per swap) equals Φ_{H₂}(x₀) − Φ_{H₁}(x₀) to rel. error < 1e-9 — K-1 is an exact identity, so any mismatch is a code bug by definition. *(Amended pre-run: an originally drafted "identical-steps commute" check was removed as vacuous — it compared a computation with itself and would pass under any bug; replaced by check (2), which is independent.)*
- **Variable changed**: none (pure instrument check).
- **Held constant**: everything; fixed seed.
- **Config**: 1-hidden-layer tanh MLP (~300 params), synthetic regression batches, SGD, float64, seed 0.
- **Degenerate-strategy baseline & expected score**: n/a (checks have exact expected values).
- **Result**: PASS after one checker fix. First run FAILED on gradcheck (1.07e-6 vs 1e-6 tol); diagnosis: FD cancellation noise at eps=1e-6 on a |g_i|≈2e-5 component — a checker artifact, not a gradient bug (same components agree to ~3e-8 at eps∈{1e-4,1e-5}; HVP bracket check independently validates gradients). With eps=1e-5: gradcheck 3.05e-8; bracket cross-check rel. err **7.0e-4** (measured pair defect vs independent FD-HVP prediction); K-1 telescoping rel. err **3.4e-17** over 21 swaps (machine precision — the exact identity verified in code, gap norm 1.5e-2).
- **Verdict**: CONFIRMED — instrument cleared. The telescoping check at float-roundoff level is the strongest possible code validation of the K-1 bookkeeping.
- **Follow-up**: none; harness is the base for E-003+ (transport operator pilots).

### E-001 (2026-07-19) — Linearity of transported defects (K-2 instrument validation, §2.2 gate rung 1)
- **Hypothesis**: for plain SGD (T = 64 steps, stable η), the tail responds linearly to the injected swap defect: R(h) := ‖Φ_A(y + h·δ) − Φ_A(y)‖/h is constant within ~5% over h ∈ [1e-3, 1] for late-training swaps, with departures growing with defect size and tail length, consistent with Lemma K-2's quadratic error term. Early-position swaps (longer tails) depart more than late ones.
- **Variable changed** (exactly one): defect scale h. (Secondary sweep, reported separately: swap position p ∈ {early, middle, late}.)
- **Held constant**: model, data multiset, η, T, seed, which pair is swapped.
- **Config**: as E-000; T = 64; commit hash recorded in results file.
- **Degenerate-strategy baseline & expected score**: h below float-noise floor excluded by monitoring absolute differences vs machine epsilon (a "perfectly constant" R(h) at noise-floor h would be a harness artifact, not linearity).
- **Falsifier**: R(h) varies > 20% across the h-range for typical late-training swaps at small η → JVP estimator invalid at pilot scale → D-010 reversal clause (windowed local fallback).
- **Result**: R(h) constant across h ∈ [1e-3, 1] to **0.019% (early, p=4), 0.0026% (middle, p=32), 0.0012% (late, p=60)** — far inside the 5% hypothesis band. Departure ordering matches the tail-length prediction (early > middle > late), though positions also differ in ‖δ‖ (2.0e-3 / 8.4e-4 / 1.2e-3), so that secondary claim is confounded and reported as consistent, not confirmed. Signal 8 orders above the float noise floor (‖moved−base‖ ≥ 1e-6 vs ~1e-14). Training non-trivial: batch loss 0.271 → 0.0227 over T=64. Incidental observation: transported-defect norms are *smaller* than the injected defects (R·1/‖δ‖ ≈ 0.36–0.81) — tails are contractive in this late-training small-model regime; relevant later to the integrable-phase story, not a claim yet.
- **Verdict**: CONFIRMED — linear response holds at pilot scale; the JVP/linear-transport estimator is valid here. Audit note on the suspicious cleanliness: values are not at a metric boundary (ratios ≠ 1 exactly, monotone in h, position-dependent) and the perturbation propagation is exact float64 replay, so near-perfect linearity at these tiny defect norms is the *expected* behavior of a smooth map, not a harness artifact.
- **Follow-up**: rerun at larger η (less contractive regime) and larger T before trusting the estimator beyond this scale — gate stays per-scale.

### E-002 (2026-07-19) — Order-defect η-scaling: SGD vs EMA momentum (first empirical contact for K-4b / F-005)
- **Hypothesis**: the two-step swap defect scales ‖δ^θ(η)‖ ∝ η^p with p ≈ 2.0 for SGD (β = 0) and p ≈ 1.0 for EMA momentum (β = 0.9, warm state), log-log slope over η ∈ [1e-4, 1e-2], fixed batches (a, b), fixed θ and seed. Predicted by Lemma K-4b: δ^θ = ηβ(1−β)(g_a − g_b) + O(η²). Additionally ‖δ^m(η)‖ has slope ≈ 0 (the state remembers order at O(1)).
- **Variable changed** (exactly one): η. Two pre-declared conditions: β ∈ {0, 0.9}.
- **Held constant**: model, θ, batches a/b, seed, momentum warm-start procedure (fixed burn-in steps).
- **Config**: as E-000; EMA momentum m⁺ = βm + (1−β)g, θ⁺ = θ − ηm⁺ (matches theory.md K-4b exactly); slopes fit by least squares on log-log with R² reported.
- **Degenerate-strategy baseline & expected score**: defect magnitudes monitored against the float64 noise floor — a slope fit through noise-dominated values is excluded; the momentum condition additionally checks the K-4b *constant and direction* (ratio and cosine against ηβ(1−β)(g_a − g_b)), not just the exponent. *(Amended pre-run: an "identical batches ⇒ δ=0" inline check was dropped as vacuous — with a = b both orders are the same computation by construction.)*
- **Falsifier**: momentum slope ≈ 2 (no first-order term) → K-4b wrong or regime inapplicable → STOP and understand before H4 proceeds (surprising-result rule).
- **Caveats declared up front**: two-step defect only (no tails — E-001 covers tails); tiny scale; not a "positive transport result," so the off-NTK constraint (design.md §5.2) does not bind here.
- **Result**: over η ∈ [1e-4, 1e-2]: SGD (β=0) slope **1.9998** (R² = 0.99999998); momentum (β=0.9) slope **1.0022** (R² = 0.999998); state-block slope **−0.020 ≈ 0** (δ^m ≈ 5.4e-3, O(1) in η). Constant-and-direction check at η=1e-4: measured δ^θ vs predicted ηβ(1−β)(g_a−g_b): **ratio 1.000117, cosine 0.99999999**. All magnitudes ≥ 4.9e-6, far above float noise.
- **Verdict**: CONFIRMED — K-4b/F-005 verified quantitatively (exponent, constant, and direction), not just qualitatively. The p≈2 vs p≈1 split is exactly the derived SGD/momentum order gap. Audit note: slopes are near-exact because the measured object is a two-step float64 computation whose leading term is an algebraic identity; the small deviations (1.0022, ratio 1.000117) are the expected O(η) subleading corrections, and the values are cross-validated against an analytically derived constant computed through a different code path.
- **Follow-up**: E-003 candidate: same sweep under Adam-style normalization (K-4d, sketch-level) — does the Θ(η) law survive the nonlinear state readout? Then first 𝓚-aware transport attempt (D-011 operator) vs degenerate baselines.

### E-003 (2026-07-19) — First 𝓚-aware transport: K-3.1 reference operator vs degenerate baselines
- **Hypothesis**: the D-011 operator T(θ(H₁)) = θ(H₁) + Σ_{(i,j)∈Inv(π)} J̄_ij δ̄_ij (single-trajectory K-3.1 reference attribution; 𝓘₁ = {π, H₁ checkpoints}; J̄δ̄ implemented as replay transport along H₁'s own trajectory — one tail replay per inversion, first-order-equal to the JVP and validated by E-001 linearity) closes most of the functional gap: r := d_f(T(θ₁), θ₂) / d_f(θ₁, θ₂) ≤ 0.2 at mild reordering (w=4 block shuffle); r is monotone non-decreasing with severity (w=4 → w=16 → full permutation, since the K-3.1 error budget grows with K and evaluation-point drift); and T beats all degenerate baselines at every severity. Secondary diagnostic: the parameter-space residual ‖T(θ₁)−θ₂‖/‖θ₁−θ₂‖ tracks r (localizes any failure to attribution error vs metric artifacts).
- **Variable changed** (exactly one): the operator applied to θ(H₁) — {K-3.1 operator, do-nothing, checkpoint-average, light fine-tune}. Secondary pre-declared sweep: permutation severity w ∈ {4, 16, full}.
- **Held constant**: model, θ₀, data multiset, η = 0.05, T = 64, seeds, eval inputs, functional metric.
- **Config**: harness as E-000 (~241-param tanh MLP, float64, data seed 42); H₁ = identity word, H₂ = π·H₁ with seeded block-shuffle permutations; d_f = RMS prediction gap on 1024 fresh eval inputs (D-006; P-1's Σ_E-style metric, isotropic input distribution); budget counted in gradient-eval-equivalents (per inversion: 4 pair evals + one tail replay) and reported against retraining = T — honestly, per D-011, even if it exceeds retraining at this K.
- **Degenerate-strategy baseline & expected score**: do-nothing is r = 1 by definition (the denominator); checkpoint-average (mean of the 𝓘₁ checkpoints the operator itself uses, plus θ₁) expected r > 1 (mid-training states drag the endpoint backwards); light fine-tune (16 SGD steps on fresh same-distribution batches, blind to θ₂) expected r ≈ 1 — if fine-tune matches the operator, the metric is not path-specific (flag, don't celebrate). Guards: do-nothing d_f must sit ≥ 6 orders above float noise; inline instrument check — for a single adjacent transposition the reference operator coincides with the exact K-1 computation, so T(θ₁) must equal θ₂ to ~1e-10 relative or the code is wrong (abort).
- **Falsifier**: r ≥ 1 at w=4 → the K-3.1 reference attribution is invalid at pilot scale → localize (parameter residual vs functional ratio; per-inversion audit) before any 𝓚 number is trusted; D-010's windowed-local fallback becomes the next rung.
- **Caveats declared up front**: tiny scale; SGD only; 𝓒_perm pairs constructed by us; η = 0.05 is far from the asymptotic regime where the O(KTη³) bound is proven small — this is exactly what the severity sweep probes; a same-data fine-tune baseline partially violates budget clause (a) but is kept as the "you retrained a bit" comparator.
- **Result**: (harness src/pilot_transport.py; raw results/raw/pilot_transport_2026-07-19.json; E-000 gate re-run PASS with values identical to the original run, confirming the `make_data` refactor left draws untouched.) Adjacent-transposition instrument check: residual **0.0** (exact, as designed). Severity sweep: **w4 (K=44): r = 0.145**, param residual 0.183; **w16 (K=218): r = 0.265**, param residual 0.472; **full (K=982): r = 0.608**, param residual **1.63**. Baselines at every severity far worse: ckpt-average r = 112 / 29 / 4.8; fine-tune r = 29.1 / 6.97 / 1.036. (The "fine-tune ≈ 1" expectation was wrong in an instructive way: 16 blind steps displace the function by ~0.043 RMS, swamping the small do-nothing gaps — blindness to θ₂ costs exactly as the framework predicts; at full severity, where gap ≈ its displacement, it lands at r ≈ 1.04.) Budget: **25× / 143× / 713× retraining** in grad-eval-equivalents (replay-transport implementation; K = O(T²) is the driver). Cancellation ratio ‖ΣΔ̄‖/Σ‖Δ̄‖ = 0.22 / 0.18 / 0.12 — heavy cancellation among transported defects. Eval losses: all conditions ≈ 0.0194 except ckpt-average (~0.04) and fine-tune (0.0182, *best loss yet wrong function* — a clean illustration that loss proximity ≠ functional identity).
- **Verdict** (confirmed / refuted / inconclusive — and why): **CONFIRMED** — all three pre-registered clauses hold: r = 0.145 ≤ 0.2 at w4; r monotone in severity; operator beats every baseline everywhere. Audit note: no boundary values (r strictly interior, varied across severities), degenerate strategies score badly as they should, and the one exact value (adjacent check) is a designed identity. Two honest additional findings: (i) at full severity the attribution **overshoots in parameter space** (residual 1.63 > 1) while still cutting the functional gap by ~40% — the metrics dissociate, supporting D-006 and locating the K-3.1 validity edge between K ≈ 218 and K ≈ 982 at η = 0.05, T = 64; (ii) transport *worked* but cost ≫ retraining at all severities — first phase-boundary data point per D-011: 𝓘₁-transport is **possible but not cheap** at this scale; no "cheap transport" claim is licensed by this experiment.
- **Follow-up**: (a) E-004: Adam-normalization η-sweep (K-4d check, already queued); (b) budget compression before any cost claim: batched forward-mode JVP (all defects share one chain ⇒ O(T) not O(KT)) + inversion sampling weighted by ‖δ̄‖ (design.md §2.2); (c) probe the validity edge: severity between w16 and full, and r(η) at fixed π.

### E-004 (2026-07-19) — Adam-normalization η-sweep: does the Θ(η) order-defect law survive the nonlinear state readout? (K-4d check)
- **Hypothesis**: for index-free, smooth-normalized Adam (m⁺ = β₁m + (1−β₁)g; v⁺ = β₂v + (1−β₂)g²; θ⁺ = θ − η·m⁺/√(v⁺+ε̂²); no bias correction per K-4a; ε̂ = 1e-8), with warm state (θ_w, m_w, v_w), the two-step swap defect scales ‖δ^θ(η)‖ ∝ η^p with **p ≈ 1.0** (log-log slope over η ∈ [1e-5, 1e-3]) in BOTH pre-declared conditions: (i) **β₁ = 0.9, β₂ = 0.9** (full Adam-class); (ii) **β₁ = 0, β₂ = 0.9** (normalization-only, RMSprop-like — the sharp test: under linear readout β₁ = 0 gives slope 2 (E-002), so slope 1 here can come only from the v-channel through the nonlinear readout, K-4d's actual mechanism claim "any C¹ readout of O(1)-differing state args"). State blocks δ^m (condition i) and δ^v (both) have slope ≈ 0 (Θ(1) order memory). Derived leading order for (ii), sharpening K-4d: δ^θ = η·β₂(1−β₂)·[N_v(g_a,v̄)(v−g_b²) + N_v(g_b,v̄)(g_a²−v)] + O(η²), elementwise, generically nonzero.
- **Variable changed** (exactly one): η. Pre-declared conditions: (β₁, β₂) ∈ {(0.9, 0.9), (0, 0.9)}.
- **Held constant**: model, θ_w, m_w, v_w (shared by both conditions), batches a/b = 0/1, seed, ε̂, burn-in procedure (θ_w: 5 SGD steps at η = 0.05 on batches [2..6], exactly E-002's; m_w: EMA β = 0.9 along that trajectory, exactly E-002's; v_w: EMA β₂ = 0.9 of g² along the same trajectory, v₀ = 0).
- **Config**: harness src/pilot_adam.py importing model/grad/data from src/pilot_defects.py; E-000 gate re-run first (abort on fail); data seed 42, T = 64 batches; η ∈ logspace(−5, −3, 7). Range note, pre-registered: the range sits 1 decade below E-002's because normalized steps are ~‖N‖/‖g‖ larger per η, and (for condition ii) the leading constant carries a (1−β₂) suppression while the O(η²) subleading does not — β₂ = 0.9 (not 0.999) is chosen for the same reason (the η-order claim is for any fixed β₂ ∈ (0,1); β₂ = 0.999 would shrink the leading constant 100× and contaminate the slope fit with subleading terms at reachable η).
- **Degenerate-strategy baseline & expected score**: all defect magnitudes monitored vs the float64 noise floor (≥ 1e-10 required; a slope fit through noise is excluded). Constant-and-direction instrument check (independent code path): frozen-θ state recursion (θ never moves; m, v recur; readouts summed) gives δ^θ_pred = η(Σ_ab N − Σ_ba N); at the smallest η require measured/predicted ratio ∈ [0.9, 1.1] and cosine ≥ 0.999, else the sweep is not in the leading-order regime and slopes are not interpretable.
- **Falsifier**: condition (i) slope ≈ 2 with the frozen-θ check passing → K-4d refuted (normalization achieves special cancellation) → STOP and understand before H4 proceeds (surprising-result rule). Condition (i) slope ≈ 1 but condition (ii) slope ≈ 2 (check passing) → K-4d's mechanism overclaims: the m-channel alone carries the first order, the v-channel does not leak — partial refutation, document honestly (H4's Adam-vs-SGD gap survives but the "any C¹ readout" argument does not). Slope anomalous with the frozen-θ check FAILING → instrument/regime problem, not a theory verdict: pre-declared diagnostic is refit on the lowest 4 η points and report both fits.
- **Caveats declared up front**: two-step defects only (no tails); tiny scale; index-free Adam variant (bias correction excluded by K-4a for 𝓒_perm compatibility — canonical Adam differs early in training); single (a, b) batch pair; leading-order claim only — constants for full Adam remain untracked (deferred per K-4d).
- **Result**: (harness src/pilot_adam.py; raw results/raw/pilot_adam_2026-07-19.json; E-000 gate re-run PASS, values identical to prior runs.) **Condition (i) β₁=0.9, β₂=0.9: slope 1.0014** (R² = 0.999999; low-4 refit 1.0002); frozen-θ check ratio **1.000046**, cosine **0.99999987**. **Condition (ii) β₁=0, β₂=0.9: full-range slope 1.202 (R² = 0.990) — contaminated at the top of the range exactly as the pre-registered range note anticipated ((1−β₂)-suppressed leading constant vs unsuppressed O(η²)); pre-declared low-4 refit: slope 1.0103** (R² = 0.99995); frozen-θ check ratio **0.99931**, cosine **0.99971** (PASS) — so the Θ(η) leading term is established at small η and the full-range curvature is the expected subleading takeover, trending toward ×3–3.4 per decade-third (between slope-1's 2.15 and slope-2's 4.64) at the top. State blocks: δ^m slopes −0.020 / −0.00004 ≈ 0, δ^v slopes −0.050 / −0.021 ≈ 0 (Θ(1) order memory in both channels). All magnitudes ≥ 3.1e-5, five orders above the declared floor. Bonus cross-check not pre-registered: condition (i) δ^m ≈ 0.00541 ≈ (1−β₁)²‖g_a−g_b‖ = 0.01 × 0.541 (condition (ii)'s δ^m) — K-4b's δ^m constant verified incidentally.
- **Verdict** (confirmed / refuted / inconclusive — and why): **CONFIRMED** — the Θ(η) order-defect law survives the nonlinear state readout in both conditions, including the sharp one: with β₁ = 0 (no momentum channel, which under a linear readout gives slope 2 per E-002), the v-channel through the normalization alone produces slope ≈ 1 at small η with the independently-predicted constant and direction. K-4d's mechanism claim ("any C¹ readout of O(1)-differing state args") stands; neither falsifier fired. Audit note: no boundary values; the frozen-θ predictor is a genuinely different code path (θ frozen, state-only recursion); the one deviation (condition (ii) full-range slope) is in the pre-registered direction with the pre-declared diagnostic applied, not post-hoc.
- **Follow-up**: theory.md K-4d updated with the derived β₁=0 leading term and the empirical-confirmation note (status stays SKETCH — constant-tracking still deferred to M5). H4's optimizer-stratification prediction now has empirical support across the full SGD → EMA → Adam-class chain at pilot scale. Next queued (unchanged): budget compression (batched JVP + inversion sampling), then the K-3.1 validity-edge probe.

### E-005 (2026-07-19) — Budget compression I: batched forward-mode JVP transport (O(K+T) grad evals vs O(KT)); quality gate vs E-003 replay
- **Hypothesis**: replacing E-003's per-inversion tail replay with the single batched forward-JVP chain (design.md §2.2's declared cheap estimator: each δ̄_ij injected into a carried vector at its position a+2, one HVP per trajectory step from the earliest injection onward, correction = chain output) **preserves transport quality** — |r_JVP − r_replay| ≤ 0.05 at every severity (E-003 replay values: 0.145 / 0.265 / 0.608) — **while cutting the operator budget ≥ 4× at every severity under E-003's counting rule (b)**, and, under D-012 rule (c) (trajectory gradients free from checkpoints, caching counted once, FD-HVP = 2 evals), the budget takes the form c₁K + c₂·(chain steps) with c₁ ≤ 3 and c₂ = 2 — i.e., the scaling changes from O(KT) to O(K + T). **Honest clause, pre-declared**: budget remains > 1× retraining at every severity at this scale under BOTH rules (the ~K pair-defect evaluations dominate; K = O(T²) here) — E-005 licenses no cheap-transport claim; its deliverable is the scaling change that makes inversion sampling (queued next) the sole remaining lever.
- **Variable changed** (exactly one): the transport mechanism — {per-inversion tail replay (E-003), batched JVP chain}. The pair defects δ̄ are computed identically (bit-for-bit) in both.
- **Held constant**: everything from E-003 — model, θ₀, data seed 42, η = 0.05, T = 64, permutation seeds 100/101/102, eval seed 7, d_f metric. Baselines (do-nothing / ckpt-average / fine-tune) not re-run; E-003's values stand.
- **Config**: src/pilot_transport_batched.py importing from pilot_defects/pilot_transport; E-000 gate re-run first (abort on fail). *(Amended pre-run, before any execution: the chain HVP is computed analytically — forward-over-reverse through the manual backprop — instead of by finite differences. Reason: an FD-HVP with normalized direction is not exactly linear in its input, which would break check (ii) as designed (it would flag FD truncation, not code bugs); the analytic HVP restores exact linearity so (ii) is a true bug detector. The FD-HVP (eps = 1e-5, the e000-validated setting) is retained as an inline instrument check OF the analytic HVP — rel. err < 1e-4 required once per severity, abort otherwise. Budget convention unchanged: one HVP = 2 grad-eval-equivalents.)*
- **Degenerate-strategy baseline & instrument checks**: (i) **per-sample audit (§2.2 validation gate)**: 10 sampled inversions per severity, individual JVP transport vs individual branched replay, median rel. err ≤ 2% required at w4/w16 (full severity: reported, no gate — defects sit at the K-3.1 validity edge); audit evals counted as instrument cost, not operator budget (D-012). (ii) **batched-vs-summed exactness**: the batched chain restricted to the audited subset must equal the sum of their individual JVP transports to ~float precision — the chain is exactly linear, so any discrepancy is a code bug (abort). (iii) correction-vector comparison vs E-003's replay correction: cosine ≥ 0.99 and norm ratio ∈ [0.9, 1.1] at w4/w16 (full: reported, no gate).
- **Falsifier**: r_JVP − r_replay > 0.05 at w4 *with* audit (i) passing → the linear estimator degrades under summation at pilot scale → D-010 reversal clause (windowed-local fallback) becomes the next rung. Audit (i) failing → FD-HVP instrument problem, not a theory verdict: pre-declared diagnostic is an eps sweep {1e-4, 1e-5, 1e-6} before any conclusion.
- **Caveats declared up front**: same tiny-scale/SGD/𝓒_perm caveats as E-003; the compression claim is about grad-eval counts under declared accounting rules (D-012), not wall-clock; retraining comparator = T = 64 evals; quality is measured against E-003's replay operator, whose own validity edge (full severity) is inherited, not cured, by the cheaper estimator.
- **Result**: (harness src/pilot_transport_batched.py; raw results/raw/pilot_transport_batched_2026-07-19.json; E-000 gate re-run PASS, identical values.) **Quality preserved two orders tighter than hypothesized**: r_JVP = 0.1457 / 0.2655 / 0.6081 vs replay-rerun 0.1452 / 0.2651 / 0.6079 (Δr = 0.0005 / 0.0005 / 0.0003; tolerance was 0.05); correction-vector cosine vs replay ≥ 0.99999, norm ratio 0.9998–1.00005 at all severities (gates: 0.99, [0.9, 1.1]). **Budget**: rule (b) 300 / 996 / 4052 evals vs replay's 1581 / 9152 / 45603 → compression **5.3× / 9.2× / 11.3×** (≥ 4× hypothesized); rule (c) 242 / 663 / 2472 → **3.78× / 10.36× / 38.6× retraining**, with the c₁K + c₂ form confirmed: pair-stage c₁ ≈ 2.4–2.7 (≤ 3 hypothesized), chain fixed at 124 evals (62 HVP steps × 2) regardless of K. Honest clause held: > 1× retraining everywhere. **Instrument checks all pass**: per-sample audit median rel. err 0.086% / 0.097% / 0.125% (gate ≤ 2% at w4/w16; max 0.25%); batched-vs-summed exactness 5.0e-16 / 5.3e-16 / 5.2e-16 (float precision, as the analytic-HVP amendment was designed to achieve); analytic-vs-FD HVP cross-check ≤ 1.8e-11. Param residual at full severity 1.631 — the K-3.1 overshoot is reproduced to 0.02% relative by the cheap estimator (E-003 replay 1.6313905 vs this run's JVP 1.6310748; agreement 2e-4, not bit-for-bit), i.e., the attribution, not the transport mechanism, is the bottleneck (as pre-declared).
- **Verdict** (confirmed / refuted / inconclusive — and why): **CONFIRMED** — all clauses: quality within tolerance (by ~100×), compression ≥ 4× at every severity, c₁ ≤ 3, scaling now O(K + T), budget still > retraining (honest clause). No falsifier fired; audit gate passed. Audit note: r values are strictly interior, differ across severities, and match E-003's independent replay implementation to 4 decimals — the agreement is expected (same attribution, validated-linear transport), not suspicious.
- **Follow-up**: the remaining cost is ~2.4 evals × K for pair defects + fixed 124 for the chain — the chain is no longer the driver; **inversion sampling (importance-weighted, unbiased) is now the sole lever for the cheap-transport question** and is the next rung (E-006 candidate; note E-003's cancellation ratio 0.12–0.22 makes naive uniform sampling high-variance — weight by ‖δ̄‖ proxy or stratify by position). Validity-edge probe (r(η) at fixed π; severity between w16 and full) still queued behind it.

### E-006 (2026-07-19) — Budget compression II: importance-weighted inversion sampling — the variance law and the sampling verdict at pilot scale
- **Hypothesis**: with the chain fixed-cost (E-005), sampling M < K inversions is the only remaining budget lever. For the with-replacement M-sample importance estimator of the correction, Σ̂ = (1/M) Σ_m Δ̄_{k_m}/p_{k_m} (unbiased for ΣΔ̄ by exact chain linearity), three pre-registered clauses:
  1. **Exact variance law (instrument clause)**: RMS relative error obeys relerr(M) = A_p/√M with A_p² = (Σ_k ‖Δ̄_k‖²/p_k)/‖ΣΔ̄‖² − 1 — an algebraic identity for i.i.d. with-replacement draws, so measured RMS over S = 100 seeds must match within ±35% per (scheme, M) cell and ±10% in median across cells; any persistent violation is a sampler code bug, not physics. Norm-weighting p ∝ ‖Δ̄_k‖ is the variance-optimal member of the importance family (Cauchy–Schwarz), so its A lower-bounds all others.
  2. **Negative headline, pre-derived from E-003's cancellation ratios** (c = ‖ΣΔ̄‖/Σ‖Δ̄‖ = 0.22/0.18/0.12): heavy cancellation makes sampling noise dominate — the oracle constant A = √(1/c²−1) ≈ 4.4/5.5/8.3, and holding median r ≤ r_full + 0.05 (r_full = E-005's 0.146/0.266/0.608) needs correction rel-err ≈ 0.14/0.17/0.19 (via the approximate ‖corr‖/gap_p mapping), i.e. **M* ≈ 1000/1000/1900 (rough, ±2×) vs K = 44/218/982 ⇒ M* > K at every severity for every scheme, oracle included** — sampling is dominated by full enumeration at this scale, and at w4 small-M sampling (M ≲ 8) is predicted *worse than doing nothing* (median r > 1). Independent pre-derivable fact registered here: under D-012 rule (c) the fixed chain alone (~124 evals) already exceeds retraining (T = 64), so no sampled operator can beat retraining at this pilot scale regardless of M — the decision quantity is **M*/K and its severity trend**, not budget < T here.
  3. **Coherence/scaling clause**: A_oracle/√K decreases with severity (pre-derived from E-003: 0.67/0.37/0.26) ⇒ the required sampling *fraction* M*/K falls as K grows; report the fitted exponent in c ~ K^(−α) (3-point E-003 estimate α ≈ 0.2) as the extrapolation quantity for where sampling could overtake enumeration at larger T. (Direction registered; the exponent value is reported, not gated.)
- **Variable changed** (exactly one): the estimator of the K-3.1 correction — full enumeration (E-005) → M-sample Monte Carlo. Pre-declared sweeps: scheme p ∝ {1 (uniform); ‖g_a‖·‖g_b‖ from trajectory gradients (free proxy — zero-cost under D-012 rule (c)); ‖δ̄_k‖ (pair-oracle, diagnostic); ‖Δ̄_k‖ (oracle, diagnostic)}; M ∈ {2, 4, 8, …} capped below K; severities w4/w16/full. Biased comparator reported gate-free: top-M truncation by ‖Δ̄_k‖ (no reweighting) — does deterministic truncation beat unbiased sampling at small M? (No prediction registered; cancellation cuts both ways.)
- **Held constant**: everything from E-005 bit-identical — model, θ₀, data seed 42, η = 0.05, T = 64, perm seeds 100/101/102, eval seed 7, d_f metric, pair-defect computation, chain code. Sampling seed family 500; S = 100 per cell.
- **Config**: src/pilot_sampling.py importing from pilot_defects/pilot_transport/pilot_transport_batched; E-000 gate re-run first (abort on fail). Per-inversion transported defects Δ̄_k computed once per severity via single-injection JVP chains (instrument cost, not operator budget, per D-012); every estimator is then a weighted subset-sum of the Δ̄ matrix — no chain re-runs inside the sampling loop. Deployed-operator budget per cell (both D-012 rules) counted from the sampled subset's cache structure + chain from the earliest sampled injection; reported as mean over seeds.
- **Degenerate-strategy baseline & instrument checks**: (i) E-000 gate re-run, abort on fail; (ii) reconstruction identity: Σ_k Δ̄_k must equal the E-005 batched-chain correction to rel ≤ 1e-10, and full-enumeration r must reproduce E-005's r_jvp — the chain is exactly linear, so any gap is a code bug (abort); (iii) the variance-law gate of clause 1 — failing cells re-run at S = 1000, persistent failure ⇒ abort; (iv) do-nothing r = 1 anchors the noise-dominated regime (small-M r > 1 is a *predicted regime*, not a bug); (v) cancellation ratios from the JVP Δ̄ matrix compared against E-003's replay values (expect close agreement; report). Functional-sensitivity mapping γ (param-noise → d_f) fitted in-run and reported with the M* extrapolations.
- **Falsifier**: any budget-honest scheme (uniform or free-proxy) reaching median r ≤ r_full + 0.05 with M < K and rule-(c) budget below enumeration's → clause 2 refuted, a *surprising positive* ⇒ STOP and understand (norm-concentration and per-sample audit) before any cheap-transport claim. Clause 1 failing after the S = 1000 recheck ⇒ instrument bug — fix before any verdict. Clause 3 non-monotone ⇒ coherence structure is severity-dependent — a finding feeding the K-3.1 validity-edge probe, not a bug.
- **Caveats declared up front**: with-replacement i.i.d. sampling only — stratified/without-replacement/control-variate designs are deferred refinements, and this rung establishes the baseline law they must beat; the rel-err↔Δr mapping behind the pre-derived M* is approximate (±2×; the in-run fitted γ supersedes it for the recorded M*); oracle and pair-oracle schemes are diagnostics, not budget-realizable operators (their weights cost what sampling is meant to save); tiny scale, SGD, 𝓒_perm as before; a negative verdict here closes *naive sampling* at pilot scale, not sampling-at-scale (clause 3 is exactly the extrapolation handle).
- **Result**: (harness src/pilot_sampling.py; raw results/raw/pilot_sampling_2026-07-19.json; E-000 gate re-run PASS, identical values; reconstruction identity Σ_k Δ̄_k vs batched chain: 6.0e-16 / 8.2e-16 / 1.5e-15; full-enumeration r reproduces E-005's r_jvp to < 1e-3.) **Clause 1 (variance law): PASS everywhere** — law ratio (measured RMS rel-err ÷ predicted A_p/√M) ∈ [0.933, 1.096] across all 84 (scheme, M, severity) cells, per-severity medians 1.005–1.010; no cell triggered the S = 1000 recheck. Cancellation ratios from the JVP Δ̄ matrix: 0.2183 / 0.1783 / 0.1198 (E-003 replay: 0.22 / 0.18 / 0.12 — agree to 3 decimals). **Clause 2: budget-honest sampling is dominated by enumeration at every severity** — fitted-γ M* extrapolations (γ = 0.779 / 0.571 / 0.366): uniform M* = 14632 / 7962 / 1370 vs K = 44 / 218 / 982 (M*/K = 333 / 36.5 / 1.4); free proxy 15808 / 6639 / 1046 (M*/K = 359 / 30.5 / 1.1). No scheme met median r ≤ r_full + 0.05 anywhere on the grid; best budget-honest cell: uniform M = 512 at full severity, median r = 0.672 vs gate 0.658 at rule-(c) budget 1221 (≈ half enumeration's 2472 — cheaper but the quality gate is still missed). At w4 the *entire* uniform grid is worse than doing nothing (median r 4.35 → 1.49 for M = 2 → 32; predicted for M ≲ 8, measured stronger). **Deviation, honestly counted**: the clause's universal quantifier ("every scheme, oracle included") fails at full severity — M*_oracle = 791 < K = 982 (M*/K = 0.81; pair-oracle 0.96): an oracle-weighted sampler would beat enumeration there by ~20%. Oracle weights are not budget-realizable (they cost what sampling saves), so the decision content stands and the falsifier did not fire. The pre-derived rough M* (1000/1000/1900) was off by up to 8.7× at w4 — the pre-declared γ-fit correctly superseded the approximate mapping. **Clause 3: CONFIRMED** — A_oracle/√K = 0.674 / 0.374 / 0.264 (pre-derived 0.67 / 0.37 / 0.26); M*/K falls ~1.0–1.3 orders of magnitude per severity step (oracle 198 → 19.6 → 0.81; uniform 333 → 36.5 → 1.4); fitted coherence exponent α = 0.19 (3-point LSQ on c ~ K^−α). The oracle crossing M*/K = 1 at K ≈ 10³ marks where sampling *begins* to beat enumeration — the deviation above is this trend's continuation, not an anomaly. Gate-free comparator: **top-M truncation (biased, oracle-ranked, no reweighting) beats unbiased sampling at almost every M** — w4 top-32: r = 0.452 vs sampled 1.42–2.56; w16 top-32: 0.744 vs 1.48–2.14; full top-512: 0.692 ≈ sampled 0.67–0.77 — under heavy cancellation, variance hurts more than bias (though non-monotone: w4 top-8 r = 1.494 > top-4's 0.984 — dropped mass is not benign either). Proxy diagnostics: Spearman(proxy, ‖δ̄‖) = 0.68–0.70, Spearman(proxy, ‖Δ̄‖) = 0.61–0.77, yet A_proxy ≈ A_uniform (w4: 6.01 vs 5.79, i.e. *worse*) — rank correlation is not variance reduction; underweighted large-norm terms inflate the 1/p_k factor.
- **Verdict** (confirmed / refuted / inconclusive — and why): **CONFIRMED — clauses 1 and 3 fully; clause 2 confirmed in its decision content, refuted in its universal quantifier.** The variance law is exact as registered (instrument clean); no budget-honest sampled operator beats enumeration at any severity at this scale — naive unbiased sampling is CLOSED as the cheap-transport lever at pilot scale (a first-class negative, per the pre-registration); the coherence trend is confirmed and now quantified (α ≈ 0.19, M*/K crossing 1 near K ≈ 10³ for the oracle). Audit note: law ratios straddle 1 on both sides (not boundary-pinned); the biggest surprise (truncation dominating sampling) occurred in the comparator that was pre-registered gate-free with no prediction, and the one refuted sub-claim is exactly the registered clause-3 trend arriving one severity earlier than the rough mapping suggested.
- **Follow-up**: (a) the verdict redirects the budget question from unbiased Monte Carlo to **structured estimators** — truncation's dominance suggests truncate-top-plus-handle-the-remainder (control-variate / stratified) designs; logged as an M5 candidate, not queued ahead of the validity edge; (b) next rung as queued: K-3.1 validity-edge probe (severity between w16 and full; r(η) at fixed π); (c) α ≈ 0.19 is a scale-extrapolation quantity — re-measure at larger T before any claim about sampling-at-scale; (d) the free proxy needs a magnitude-calibrated (not rank-calibrated) form before it is worth another run.

### E-007 (2026-07-19) — K-3.1 validity-edge probe: where the attribution overshoot begins (K-interpolation at fixed η) and whether it is an η-artifact (η-sweep at fixed π)
- **Hypothesis**: the full-severity parameter overshoot (residual ‖T(θ₁)−θ₂‖/‖θ₁−θ₂‖ = 1.631 at K = 982, η = 0.05 — reproduced bit-faithfully by both transport mechanisms, so it is attribution error per F-010) is (i) **K-accumulated**: along a single structural family of permutations with exactly controlled inversion count — bubble-sort *prefixes* of the reduced word from identity toward the seed-102 full permutation, where a prefix of s adjacent swaps has exactly K = s inversions, all a subset of the full permutation's — the residual ρ(K) is monotone non-decreasing and crosses 1 at some K_c ∈ (218, 982) (rough point prediction if accumulation is linear-in-K between the measured endpoints: K_c ≈ 550–600); the functional ratio r(K) is monotone non-decreasing and stays < 1 throughout (transport still cuts the gap even past the parameter-space edge — the E-003 dissociation persists); and (ii) **η-suppressed at fixed π**: at the full permutation, K-3.1's neglected terms are one power of η beyond the leading defect, so ρ(η) decreases monotonically as η falls, roughly linearly (ρ ≈ 1.63·η/0.05, ±2×, i.e. ~0.8 at η = 0.025 and ~0.4 at η = 0.0125, crossing ρ = 1 between η = 0.025 and 0.05); r(η) also decreases monotonically. If (ii) holds, the validity edge is a (K, η) frontier, not a K-wall — the K-3.1 attribution is asymptotically exact at fixed severity.
- **Variable changed** (exactly one per sub-sweep): (a) inversion count K via prefix length s ∈ {218, 350, 500, 650, 800} at fixed η = 0.05 (s = 218 doubles as a structure probe against w16's K = 218: same count, different inversion set — weak prediction, report-only, no gate: the prefix family front-loads long-range inversions, so ρ(s=218) > w16's 0.472); (b) η ∈ {0.0125, 0.025} at the fixed seed-102 full permutation (η = 0.05 is the E-005 reference). Nothing else changes.
- **Held constant**: model, θ₀, data seed 42, T = 64, eval seed 7, d_f metric, batched-JVP transport mechanism and pair-defect computation (bit-identical to E-005); in (a) η = 0.05; in (b) the permutation word.
- **Config**: src/pilot_edge.py importing from pilot_defects/pilot_transport/pilot_transport_batched; E-000 gate re-run first (abort on fail). Inline instrument checks: each prefix word must satisfy len(inversion_pairs(word)) == s exactly (reduced-word property — abort if not); per-sample audit (10 inversions, individual JVP vs individual branched replay) at every cell — gate median rel err ≤ 2% at cells with ρ < 1, report-only at overshoot cells (E-005 convention); gap_f monitored ≥ 1e-8 above float noise (η = 0.0125 shrinks defects ~16×; a noise-floor gap voids the cell, pre-declared). Budgets reported under both D-012 rules (no new accounting).
- **Degenerate-strategy baseline & expected score**: do-nothing r = 1 is the denominator; no operator-vs-baseline re-litigation (settled in E-003) — the subject is the operator's own error structure. Reference anchors that must reproduce: full-severity cell at η = 0.05 equals E-005's (r = 0.608, ρ = 1.631).
- **Falsifier**: (a) ρ non-monotone in K along the prefix family, or r > 1 anywhere at η = 0.05 → attribution error is not simply accumulated with inversion count — STOP and localize (which added inversion band broke it) before any validity-edge claim; (b) ρ(η) NOT decreasing at fixed π → the overshoot is a genuine K-wall, not an η-artifact — a *meaningful refutation* (strengthens the path-burned reading of high severity and caps K-3.1's usable range independent of step size); document as a first-class negative, not a bug.
- **Caveats declared up front**: prefix family and block-shuffle family differ in inversion structure, so K_c is family-relative (the s = 218 vs w16 comparison measures exactly this); η-sweep changes the whole trajectory (θ₁, θ₂, gap all move — the claim is about the ratio ρ, not raw distances); two-point η-sweep gives a slope estimate, not a scaling law (log-spaced third point deferred unless the two disagree with the linear prediction by > 2×); tiny scale, SGD, 𝓒_perm as before.
- **Result**: (harness src/pilot_edge.py; raw results/raw/pilot_edge_2026-07-19.json; E-000 gate re-run PASS, identical values; reference anchor reproduces E-005 exactly — r = 0.6081, ρ = 1.6311; all prefix words passed the exact-inversion-count check; every audit median ≤ 0.13% vs the 2% gate; all gaps ≥ 5.2e-3, far above the noise floor.) **(a) K-sweep at η = 0.05 (prefix family)**: ρ = 0.319 / 0.562 / 0.695 / 1.128 / 1.280 at s = 218 / 350 / 500 / 650 / 800 (full: 1.631) — **monotone; crossing K_c ≈ 606** (linear interpolation between 500 and 650), at the top edge of the pre-registered 550–600 band. r = 0.133 / 0.210 / 0.223 / 0.364 / 0.461 (full: 0.608) — monotone and < 1 throughout: transport still cuts the functional gap by ≥ 39% even past the parameter-space edge — the E-003 dissociation persists as hypothesized. **Structure probe (report-only clause): direction REFUTED** — prefix-family ρ(K=218) = 0.319 vs block-shuffle w16's 0.472 (and r = 0.133 vs 0.265): same inversion count, ~33% *smaller* residual, opposite the weak guess. K is not a sufficient statistic for the edge; the prefix family (few displaced elements, each generating many correlated inversions) attributes *better* than scattered same-count block shuffles. **(b) η-sweep at fixed full π**: ρ = 0.485 / 0.878 / 1.631 at η = 0.0125 / 0.025 / 0.05 — monotone decreasing as η falls, crossing ρ = 1 between 0.025 and 0.05 as predicted, and close to the linear prediction (ρ/η = 38.8 / 35.1 / 32.6 ≈ constant; predicted 0.41 / 0.82 vs measured 0.485 / 0.878 — within 20%, far inside the ±2× band; log-log slope 0.875). r(η) = 0.350 / 0.483 / 0.608 — monotone decreasing ✓. Audit errors also shrink with η (3.1e-5 at η = 0.0125), consistent with K-2. **Post-hoc observation, explicitly NOT pre-registered and NOT a claim**: all eight prefix/full-family cells fit ρ ≈ 0.033·K·η within ~±12% (both log-log slopes ≈ 1: 1.08 in K, 0.87 in η) — a candidate closed-form edge law (ρ = 1 at K·η ≈ 30 at this T and family; w16's block-shuffle point sits 30% off it, confirming the family-dependent prefactor). To be pre-registered against new cells (different T, third η, other families) before any use.
- **Verdict** (confirmed / refuted / inconclusive — and why): **CONFIRMED — both gated sub-hypotheses in full; the one refuted clause was the pre-declared report-only structure guess.** (i) The overshoot is K-accumulated along a fixed family: monotone, K_c ≈ 606 ∈ (218, 982), r < 1 everywhere at η = 0.05. (ii) The overshoot is η-suppressed at fixed π — approximately Θ(η) — so the K-3.1 validity edge is a **(K, η) frontier, not a K-wall**: the attribution is asymptotically exact at fixed severity, and the full-severity failure of E-003/E-005 is a finite-step-size effect, not a structural limit of the reference attribution. Neither falsifier fired; no boundary values (crossings interior, interpolated); the anchor and audit checks tie every cell to the validated E-005 instrument.
- **Follow-up**: (a) pre-register the ρ ≈ c·K·η law (c ≈ 0.033 here) as a gated clause in the next transport experiment that varies T or adds a third η — it is the quantitative form of the phase-boundary frontier for the K-3.1 operator and, if it survives, gives 𝓚-side meaning to "severity × step size" as the edge coordinate; (b) the structure-dependence result (prefix ≠ block shuffle at matched K) says the edge law's prefactor carries inversion-set geometry — candidate theory hook for Π_⊥/attribution refinement (M5); (c) M3.1 pilot ladder is now complete through the validity edge — next: M3.1 wrap-up synthesis + professor_brief refresh (F-010..F-012), then M3.2 scoping (PolyPythias pair selection).

### E-008 (2026-07-20) — Pre-registered test of the F-012 edge law ρ = c·K·η: out-of-grid point predictions + T = 128 form test
- **Hypothesis** (law and prefactor FROZEN from E-007 raw data before any new cell is run; c = 0.032918, least-squares through origin of ρ on K·η over E-007's 8 prefix/full-family cells):
  - **(i) Gated, T = 64, point predictions**: the frozen law predicts the parameter residual ρ at three cells OFF the E-007 fitting grid — (K=350, η=0.025): ρ̂ = 0.288; (K=650, η=0.025): ρ̂ = 0.535; (K=982, η=0.00625): ρ̂ = 0.202 — each within **±25 % relative error**, and with lower median |rel. err.| than BOTH pre-declared nulls fit to the same 8 cells by the same procedure: null-A "K-only" ρ = a·K (a = 0.0012140) and null-B "η-only" ρ = b·η (b = 19.566). Note null-A is only distinguished from the law at the η=0.00625 cell (predicts 1.19 vs law's 0.20) — that cell is the discriminating one; null-B fails the K-dependence at both η=0.025 cells (predicts 0.489 flat).
  - **(ii) Gated, T = 128, form test**: in a fresh run (new data, new init, new full permutation), prefix-family cells at K ∈ {300, 600, 900, 1200}, η = 0.05: ρ is linear in K through the origin — centered R² ≥ 0.98 for the through-origin fit ρ = m·K, and ρ strictly increasing in K. c(128) = m/η is a **measurement** (first estimate of the T-dependence of the prefactor), explicitly NOT predicted; report c(128)/c(64).
  - **(iii) Secondary, not gated**: r < 1 at every cell (functional transport keeps surviving past/near the parameter edge, extending F-012's dissociation).
- **Variable changed** (exactly one): the evaluation cells — everything about the law (form, prefactor, fitting procedure, nulls) is frozen from E-007's already-committed raw JSON before the run. (The T = 128 arm necessarily changes data quantity/init with T; it is a form test only — no cross-T constant comparison beyond reporting the ratio.)
- **Held constant**: harness bit-identical to E-007 (`transport_cell`, batched forward-JVP chain, exact HVP, audit gates per E-005 convention); T=64 arm: θ0/data (seed 42), eval set (seed 7), seed-102 full word and its bubble prefixes; η_ref = 0.05 for arm (ii).
- **Config**: `src/pilot_edge2.py` (imports E-007 machinery), tanh MLP ~241 params, SGD, float64. T=64 arm: prefix words at s = 350, 650 run at η = 0.025; full word (K = 982) at η = 0.00625. T=128 arm: rng(43) → batches + θ0, full word seed 103, prefixes at s ∈ {300, 600, 900, 1200}, η = 0.05. E-000 self-check gate runs for BOTH arms before subject cells.
- **Degenerate-strategy baseline & expected score**: null-A and null-B above (a degenerate "law" that ignores one variable). If the K·η law is real, its median |rel. err.| beats both; if ρ is really a K-wall or a pure-η effect, the corresponding null wins and clause (i) fails.
- **Falsifier / downgrade path (pre-declared)**: any gated clause fails → the quantitative law ρ = c·K·η is REFUTED as stated; F-012 §3 downgrades to the directly measured facts (ρ monotone in K at fixed η; ρ ≈ Θ(η) at fixed K) and the refutation is logged as a first-class negative in findings.md. No refitting c on the new cells to rescue the gate.
- **Void conditions**: functional gap at noise floor (gap_f ≤ 1e-8); per-sample audit gate failure at a non-overshoot cell (cell void, experiment continues; >1 void cell → inconclusive).
- **Result** (raw: results/raw/pilot_edge2_2026-07-20.json, commit fdb0104): E-000 gates passed for both arms (telescoping 3.4e-17 / 7.8e-18); no void cells; per-sample audits passed everywhere (median JVP-vs-replay err ≤ 0.16%).
  - **(i)**: (K=350, η=0.025): ρ = 0.2627 vs ρ̂ = 0.2880 (−8.8%); (K=650, η=0.025): ρ = 0.5541 vs 0.5349 (+3.6%); (K=982, η=0.00625): ρ = 0.2534 vs 0.2020 (**+25.4% — fails the ±25% gate by 0.4 pp, on exactly the discriminating cell**). The frozen law beat both nulls decisively: median |rel err| 8.8% vs null-A 38.2% and null-B 46.3%. `all_within_25pct` false → **clause (i) FAILS**.
  - **(ii)**: T=128 fresh run (seed-103 word, K_full = 4171): ρ = 0.1005 / 0.2669 / 0.4521 / 0.5841 at K = 300/600/900/1200 — strictly increasing, but centered R² = **0.9791 < 0.98** → **clause (ii) FAILS** (real curvature, not noise: ρ/K = 3.35 / 4.45 / 5.02 / 4.87 ×10⁻⁴ rises then dips). Measured c(128) = 0.00962, so **c(128)/c(64) = 0.292** — the prefactor is strongly T-dependent.
  - **(iii) secondary**: r < 1 at all 7 cells, max r = 0.263 — functional transport survival extends to T = 128 and to the smallest η tested.
  - η-drift diagnosis (K = 982 column across E-007+E-008): effective c = ρ/(K·η) = 0.0332 / 0.0358 / 0.0395 / 0.0413 as η falls 0.05 → 0.00625; local log-log slope 0.86–0.94 — η-suppression is slightly sub-linear, which is what pushed the small-η cell over the gate.
- **Verdict**: **REFUTED as stated** — both gated clauses failed, each at the margin; the pre-declared downgrade fires with no refit rescue (c was not refit on new cells). The *structure* survives everything we threw at it (beats both nulls, monotone in K, near-linear, Θ(η)-ish), but ρ = c·K·η with a universal c is dead twice over: sub-linear η drift and, decisively, the T-dependent prefactor. K·η is not the frontier variable.
- **Follow-up**: F-013 logged (first-class negative + the T-dependence discovery); F-012 §3 downgraded via supersession marker. Post-hoc candidate for a future pre-registration (NOT a claim): inversion-*fraction* law ρ = ĉ·(K/K_max)·η with K_max = T(T−1)/2 — ĉ = c·K_max comes out 66.4 (T=64) vs 78.2 (T=128), within 18% where raw c differs 3.4×; needs a third T and a declared η-exponent before it may be claimed. Deferred behind M3.2 (E-009 per D-013 is next).

### E-009 (2026-07-20) — First real history pairs: D-013 verification gates + checkpoint-matched divergence curves for order-only / init-only / joint PolyPythias 160M pairs
Pre-registered 2026-07-20 BEFORE any checkpoint download or evaluation. Two phases: phase 0 is an instrument/identity check (gates from D-013); phase 1 is the subject measurement. Phase 1 runs only if gates (i) and (ii) pass.

- **Pairs (D-013)**: P1 = (`EleutherAI/pythia-160m-data-seed1`, `-data-seed2`) — order-only; C1 = (`-weight-seed1`, `-weight-seed2`) — init-only; J1 = (`pythia-160m-seed1`, `-seed2`) — joint. All 160M, 300B Pile tokens, checkpoints as git branches `stepN`.
- **Hypothesis**:
  - **Phase 0, gated**: (G-i) `step0(data-seed1) == step0(data-seed2)` bitwise (`torch.equal` per tensor) — same init by construction; (G-ii) `step0(weight-seed1) != step0(weight-seed2)`. Not gated, recorded: (G-iii) classify `step0(data-seedN)` vs `step0(pythia-160m)` (equal ⇒ main run joins P1's equivalence class, bonus pairs); (G-iv) optimizer/LR config read from the EleutherAI/pythia repo configs and model cards, not assumed — expectation (non-gated): Adam, peak LR 6e-4, ~1% warmup, cosine to 10% of peak. **If G-i fails, D-013 is superseded (pair moves to the homotopy track) and phase 1 does not run under this registration.**
  - **Phase 1, gated**:
    - **(H-a) Separation of scales at end of training**: floor < d_f(X, 143000) for all X ∈ {P1, C1, J1}, and d_f(P1, 143000) < ceiling, d_f(C1, 143000) < ceiling, where floor = same-run adjacent-checkpoint drift d_f(data-seed1@142000, data-seed1@143000) and ceiling = cross-size gap d_f(pythia-160m@143000, pythia-410m@143000). J1-vs-ceiling is recorded but NOT gated (J1 is the reference pair; seed spread rivalling the size gap would be a finding, not a failure).
    - **(H-b) Joint dominance, weak form**: d_f(J1, t) ≥ 0.8 · max(d_f(P1, t), d_f(C1, t)) at every matched checkpoint t ≥ 1000 (0.8 tolerance for single-seed noise).
    - **(H-c) Order-vs-init accrual timing** (the theory-flavored prediction; K-4b/K-4d hook — order holonomy accrues ∝ η(t) along training, so with warmup+cosine LR order-divergence should still be accumulating at mid-training, while init-divergence is present from step 0): m(P1) < m(C1), where m(X) := d_f(X, 16000) / d_f(X, 143000) (mid-training fraction of final divergence; continuous statistic, strict inequality). Descriptive corollary (not gated): d_f(C1, t) may be non-monotone (large at t=0, functional convergence under identical data order); d_f(P1, 0) must be ≈ 0 (sanity anchor, follows from G-i).
  - **Secondary, not gated**: d_θ(X, t) (relative L2) recorded alongside d_f at every cell — divergence/recoverability context only (D-006: functional is the claim-bearing metric); curve shapes vs the LR schedule; whether d log d_f/dt for P1 falls in the final third (cosine decay).
- **Variable changed** (exactly one): which training factor differs within the pair — data order only (P1) vs init only (C1) vs both (J1). Model size, architecture, data multiset, tokenizer, optimizer config, checkpoint grid, eval shard, metric all matched across pairs.
- **Held constant**: checkpoint grid t ∈ {0, 1, 4, 16, 64, 256, 512, 1000, 4000, 16000, 64000, 128000, 143000} (13 points; 0 is the free gate-phase anchor, descriptive only); eval shard = first 48 non-overlapping 2048-token blocks of `NeelNanda/pile-10k` concatenated in dataset order with EOS separators, tokenized by the pythia-160m tokenizer (~98k tokens; Pile *train*-distribution sample, NOT held-out from training — acceptable because d_f compares two models against each other, not against memorization; caveat travels with the number); all evals fp32 (upcast from storage dtype, same treatment both sides), same device for every cell, `torch.use_deterministic_algorithms(True)` if feasible, device + dtype recorded in raw JSON.
- **Config**: `src/e009_gates.py` (phase 0), `src/e009_divergence.py` (phase 1). d_f := mean over tokens of symmetric KL, 0.5·KL(p_A‖p_B) + 0.5·KL(p_B‖p_A), nats, full next-token distributions; also recorded: top-1 disagreement rate, per-model CE loss and loss gap. d_θ := ‖θ_A−θ_B‖₂ / (0.5(‖θ_A‖₂+‖θ_B‖₂)) over all parameters. Floor anchors (descriptive beyond H-a): (1000,2000), (16000,17000), (142000,143000) on data-seed1. Budget: ~25 GB checkpoint downloads (evictable cache; raw JSONs are the record), forward evals only, no training, no gradient computation.
- **Degenerate-strategy baseline & expected score**: the floor IS the degenerate reading — if any pair's end-of-training d_f is indistinguishable from same-run adjacent-checkpoint drift, the "divergence" is measurement-scale noise and no structural claim survives. Instrument check: one pre-declared cell (P1 @ 16000) evaluated twice; symmetric KL must reproduce to rel. 1e-6.
- **Falsifier / downgrade path (pre-declared)**: H-a fails → divergence curves are not interpretable at this eval size; report as instrument limitation, no pair claims. H-b fails → single-factor divergence exceeds joint variation — a first-class structural surprise, logged in findings.md as such. H-c fails → the η-coupling story for order-divergence is refuted at real scale; K-4b/K-4d carry-over claim in D-013 gets a supersession remark. No post-hoc re-choice of the mid-training checkpoint, tolerance, or eval shard to rescue any gate.
- **Void conditions**: missing/undownloadable checkpoint branch or weight file → cell void; >2 void cells in any pair's curve → that pair inconclusive; reproducibility check fails (rel. > 1e-6) → experiment inconclusive pending determinism fix.
- **Result — phase 0** (raw: results/raw/e009_gates_2026-07-20.json; step0 of 5 repos, torch 2.13.0+cu126):
  - **G-i PASS**: `step0(data-seed1)` and `step0(data-seed2)` are bitwise identical — all 184 tensors `torch.equal`, and the two `pytorch_model.bin` files share the same SHA-256 (`6654b4ea…`). The order-only pair P1 is confirmed inside 𝓒_perm's closure; D-013 stands.
  - **G-ii PASS**: `step0(weight-seed1) != step0(weight-seed2)` — exactly 50 of 184 tensors differ (max |Δ| = 0.184), and the 50 are precisely the randomly-initialized matrices (12 layers × {QKV, attn-dense, mlp-in, mlp-out} + embed_in + embed_out); the 134 equal tensors are constant-init LayerNorm/bias parameters and non-learnable buffers. Init-only control C1 confirmed.
  - **G-iii (recorded)**: main `pythia-160m` step0 stores fp32 safetensors with 148 tensors (no buffers); the decoupled runs store fp16 bins with 184 (36 extra non-learnable buffers: causal masks, `inv_freq`, `masked_bias`). **All 148 common parameter tensors are bitwise equal between main and both data-seeds** (max |Δ| = 0.0) — the script's strict `same_keys` flag is false only because of the buffer tensors. Classification: **the main run joins P1's equivalence class** — (main, data-seed1) and (main, data-seed2) are bonus order-only pairs. vs weight-seed1: same 50 tensors differ, consistent. **Caveat (added post-F-014)**: this equivalence-class membership was gated on `step0` identity *only* — exactly the gate blind spot F-014 exposes (the decoupled checkpoints do not continue from their own `step0` inits at later steps). It certifies shared init, **not** continuity of the main run's trajectory with the data-seed trajectory beyond `step0`; the bonus order-only pairs inherit F-014's `step≥1` caveat and are not usable past `step0` without a continuity gate. **Recorded-boolean reconciliation**: the raw verdict `G_iii_main_in_P1_class = false` in results/raw/e009_gates_2026-07-20.json is a *storage-format artifact*, not a refutation — it reflects the safetensors (148-key) vs `.bin` (184-key) keyset mismatch through the strict `same_keys` test (recorded `same_keys=false`, `max_abs_diff=0.0` for both `gate_iii_ds{1,2}_vs_main`); all 148 common parameters are bitwise identical, so the substantive claim above stands over the raw boolean. (A parallel change fixes the script's verdict semantics; this entry documents only the recorded-JSON discrepancy.)
  - **G-iv (recorded, not assumed)**: `models/160M/pythia-160m.yml` from EleutherAI/pythia (saved to results/raw/e009_training_config_pythia-160m.yml): optimizer **Adam**, betas [0.9, 0.95], eps 1e-8, peak lr 6e-4, warmup 0.01 (= 1430 steps of 143000), lr-decay-style cosine, min_lr 6e-5, weight-decay 0.1, train-iters = lr-decay-iters = 143000, seq-length 2048. Matches the non-gated expectation exactly.
  - Phase-1 precondition (G-i ∧ G-ii) satisfied → phase 1 launched under this registration.
- **Result — phase 0b, checkpoint trajectory-continuity audit** (added 2026-07-20 AFTER phase 1 was interrupted, prompted by the `C1@1` reading below; harness `src/e009_audit_checkpoints.py`, raw results/raw/e009_ckpt_audit_2026-07-20.json; read-only, offline against the existing HF cache, no new download):
  - **Why it was needed**: the phase-0 gates (G-i..G-iv) only ever inspected `step0`. That is precisely the one revision where the decoupled repos are well-formed, so the gates could not see the defect below. Gate-design error, logged as such in D-014.
  - **G-v (trajectory continuity) FAIL.** Define cont(R, t) := d_θ(R@step0, R@t) / d_θ(R@step0, ds1@step0) — how far a run has moved from its own init, in units of the distance between two independent inits. For a genuine continuation cont ≈ 0 at small t. Measured: **cont(weight-seed1, t) = 1.0000, 1.0000, 0.9999, 1.0000, 1.0011 at t = 1, 4, 16, 64, 256** and the same to 4 decimals for weight-seed2. The weight-seed runs are at full independent-init distance from their own `step0` at *every* subsequent checkpoint.
  - **G-vi (pair non-degeneracy) FAIL** at `step1`: `P1@step1` and `C1@step1` both have d_θ = 0 over all 148 parameter tensors. **At `step1` all four decoupled repos are numerically identical**: d_θ(ds1@1, ws1@1) = 0 exactly — a *same-revision* cell (the recorded audit cell `ds1|ws1@step1` compares ds1@step1 with ws1@step1, not step0 with step1). That this common `step1` state also equals the shared `step0` data-seed init is an **inference**, not a directly measured cross-revision cell, supported by the identical cross-entropy at `P1@0` and `P1@1` (both 11.067606399354746).
  - **Structural reading**: at `step0` there are three distinct inits, mutually 1.2475 apart (ds shared, ws1, ws2). From `step1` onward every run is a trajectory descending from the *data-seed* init; the weight-seed inits are orphans that no later checkpoint continues. Cross-family distances confirm one family: at step4 all six pairs sit at 4.7–5.3e-5, at step256 at 1.4–2.6e-2 — cross-family (ds↔ws) is the same order as within-family (ds1↔ds2), not the ~1.25 an init difference would impose.
  - **The decisive argument needs no new instrument**: C1's d_θ goes **1.2475 at step0 → 4.69e-5 at step4**. Two runs from independent random inits cannot converge four orders of magnitude in four optimizer steps. Whatever the upstream cause, the weight-seed `step≥1` checkpoints are not continuations of the weight-seed `step0` inits.
  - **Cache poisoning excluded**: the `step1` files have distinct commit SHAs and distinct whole-file SHA-256s, differing only in **signed-zero bits** (−0.0 vs +0.0) in 5 tensors — the signature of independent uploads of identical numbers. A locally duplicated file would be byte-identical. Method note now baked into the harness: these comparisons must be numerical (`torch.equal` / rel-L2), never byte hashes — an early audit script hashed raw bytes and mis-reported these numerically-identical pairs as *different*, because −0.0 ≠ +0.0 bytewise. Buffers (`masked_bias` = −inf, causal masks, `inv_freq`) are excluded; including them poisons every norm with inf/nan.
- **Result — phase 0b-J1 (2026-07-21), J1 continuity audit** (harness `src/e009_audit_checkpoints.py --set=j1`, new selectable repo set — default set unchanged for reproducibility of the 2026-07-20 run; raw results/raw/e009_ckpt_audit_j1_2026-07-21.json; `pythia-160m-seed1`/`-seed2` at step0/1/4, ds1 as distinct-init reference; ~2 GB new downloads, read-only, per the D-014 cheapest-first recommendation):
  - **G-v PASS**: cont(seed1, 4) = 5.63e-5, cont(seed2, 4) = 5.63e-5 — both seed runs continue from their own `step0` inits (weight-seed runs measured 1.0000 on the identical gate).
  - **G-vi PASS**: seed1|seed2 differ at every revision, rel d_θ ≈ 1.2474 (50 differing tensors at step0 — exactly the randomly-initialized matrices, the G-ii signature; 123 at step4).
  - **Quirk recorded**: `step1` ≡ `step0` bit-for-bit within each seed repo (continuity ratio exactly 0.0) — the `step1` revision is an init copy, not a post-step checkpoint. No grid impact (`step1` already dropped, see below), but goes in any upstream report.
  - **Consequence**: F-014 is confined to the decoupled repos; **J1 is cleared for phase 1** (step0 and step4+; audited to step4 only — the phase-1 J1 curve itself re-checks continuity for free, since a defective J1 would sit at ~1e-2 from the ds family instead of ~1.25). F-014 addendum logged same day.
- **Grid amendment (2026-07-21, instrument)**: `step1` removed from the phase-1 GRID in `src/e009_divergence.py` — void upstream at that revision for every repo family (F-014 + the J1 quirk above). Recorded 2026-07-20 step1 cells stay in the JSON untouched. 12 grid points remain.
- **Result — phase 1 (PARTIAL, interrupted)** (raw: results/raw/e009_divergence_2026-07-20.json; cuda, torch 2.13.0+cu126, `use_deterministic_algorithms(True)`, 48×2048 = 98,304 eval positions per cell):
  - Coverage: all 13 grid points for P1; C1 through `@256` only. C1's tail, all of J1, all floor anchors, the ceiling and the reproducibility cell were never reached. **The pre-declared reproducibility check (P1@16000 twice, rel. 1e-6) has NOT been run**, so no phase-1 number is instrument-certified yet.
  - **Why it stopped — interrupt, not error.** `weight-seed2@step512`'s cache snapshot holds only `config.json` beside a **0-byte `.incomplete` blob** timestamped 19 s after the JSON's last write. The harness catches `Exception` per cell and records a `void` entry, so a download/network failure would have left a record; none exists. `KeyboardInterrupt` derives from `BaseException` and bypasses that handler — consistent with Ctrl-C or a hard kill. The script is resumable and skips completed cells; the missing cells cost only their own compute.
  - **The metric instrument is clean — verified, not assumed.** `model.state_dict()` exposes exactly 148 keys, all trainable parameters, zero buffers, zero non-finite values, so `d_theta_rel` never touches `masked_bias`. An independent recomputation from the checkpoint files reproduces the recorded JSON bit-for-bit: `P1@4` 2.438513e-05, `C1@0` 1.247474, `C1@4` 4.689178e-05. The `sym_kl = 0` readings are *correct measurements of degenerate inputs*, not a harness bug.
  - **P1 (order-only) — usable from `@4` onward.** `P1@0` = 0 is the G-i sanity anchor as pre-declared. `P1@1` is **void** (degenerate upstream checkpoint, G-vi). `P1@4…@143000` behave as a divergence curve should: sym_kl rises 9.2e-7 → 0.317 peaking near `@4000`, then settles to 0.290 at `@143000`; d_θ_rel rises 2.4e-5 → 0.954; top-1 disagreement peaks at 0.300 (`@512`) and relaxes to 0.233. **Correction 2026-07-22**: d_θ_rel was described here as rising "monotonically" — it does not; it peaks at 0.965503 (`@16000`) and eases to 0.953915 by `@143000` (0.9655 → 0.9640 → 0.9609 → 0.9539 over the last four cells). Verified against the raw JSON. Nothing gated on it. Not yet gated — H-a needs the floor and ceiling cells, which were never run.
  - **C1 (init-only) — no valid data beyond `step0`.** The arm's defining contrast exists only at `@0`; every C1 cell at t ≥ 1 compares two runs that share the data-seed init. **H-b and H-c have no control arm.** H-c in particular is unrunnable as registered: it contrasts order-divergence against init-divergence accrual, and there is no init-divergence curve.
  - **Unexplained and recorded, not claimed**: from `step1` on, ws1 and ws2 share an init yet diverge (1.38e-2 at `@256`), and consistently *less* than the ds pair (2.52e-2). Per D-013 the weight-seed runs were supposed to hold data order *fixed*; identical init plus identical order would force bit-identity, which is not observed. What distinguishes ws1 from ws2 downstream is therefore undetermined. They are **not** promotable to bonus order-only pairs without a further gate.
- **Verdict (2026-07-20, interrupted state — superseded by the resume verdict below)**: phase 1 **INCONCLUSIVE — arm C1 void beyond `step0`, run incomplete.** No H-a/H-b/H-c gate is evaluated; nothing from this run may be reported as a pair claim. P1's partial curve survives as instrument-verified but ungated data. Per the pre-registered no-post-hoc-rescue rule, H-b/H-c are not to be re-specified against the same data to make them runnable; a replacement control must be pre-registered separately.
- **Result — phase 1 resume (2026-07-21), COMPLETE under the amended registration** (same raw file results/raw/e009_divergence_2026-07-20.json, appended; cuda, deterministic; 17 new cells — J1 ×12, floors ×3, ceiling, repro; **36 cells total, zero void**):
  - **Reproducibility cell PASS**: `repro:P1@16000` reproduces `P1@16000` with rel. err **exactly 0.0** (gate ≤ 1e-6). Boundary-value note (instrument-skepticism discipline): 0.0 is the *designed* outcome under `torch.use_deterministic_algorithms(True)` on the same device with identical inputs — the cell genuinely recomputed all 96 forward passes (fresh model loads, same wall-time as neighboring cells), so bitwise-identical output certifies determinism rather than indicating a copied result. Phase-1 numbers are now instrument-certified.
  - **J1 tripwire NOT tripped (arm valid)**: d_θ(J1) = 1.2474 / 1.2474 / 1.2495 / 1.2597 / 1.2829 at t = 4/16/64/256/512/1000 — full independent-init scale everywhere in the window (threshold 0.5); the F-014 continuity inference for J1 beyond step4 is now backed by measurement across the whole grid (d_θ never drops below 1.24).
  - **H-a PASS** (amended: C1 clauses void): floor(142000–143000) = 0.076275 < d_f(P1@143000) = 0.290461 (**3.81×**) and < d_f(J1@143000) = 0.306112 (4.01×); d_f(P1@143000) < ceiling = 0.460360. Recorded, not gated: J1@143000 / ceiling = 0.665 — the two-seed spread at 160M is about two-thirds of the 160M↔410M size gap in sym-KL.
  - **H-b′ PASS at all six gated checkpoints** (t ≥ 1000): d_f(J1)/d_f(P1) = 1.831 / 1.167 / 1.073 / 1.060 / 1.007 / 1.054 at t = 1000/4000/16000/64000/128000/143000 — all ≥ 0.8. Joint variation weakly dominates order-only variation throughout, as the original H-b predicted for the surviving arms.
  - **Headline descriptive structure (not a gate; F-015)**: the order-only fraction of joint divergence, d_f(P1,t)/d_f(J1,t), **rises from ≈0 at t ≤ 16 through 0.319 (t=256), 0.546 (t=1000), 0.857 (t=4000), 0.932 (t=16000) to 0.949–0.993 late**  (**correction 2026-07-22**: originally written "rises monotonically"; the rise is monotone at 10 of 12 grid points but the final step ticks down, 0.9934 at t=128000 → 0.9489 at t=143000 — verified against the raw JSON, no gate depended on it) — by end of training, data order alone reproduces ~95% of the functional divergence that order+init jointly produce. In d_θ the pairs stay on independent scales throughout (P1 → 0.954; J1 1.247 → 1.408), so this is a functional-space statement, not parameter-space convergence.
  - **Floor caveat that travels with the curve**: the floor is strongly t-dependent — adjacent-checkpoint drift is 0.680 (1000→2000), 0.141 (16000→17000), 0.076 (142000→143000). Early drift *exceeds every pair value at any t*, so separation-of-scales is a **late-training statement**; early-t cells (t ≲ 1000) sit below the local drift scale and support no pair claims on their own. Scale context: two *untrained* independent inits measure sym_kl = 0.3995 (J1@0), comparable to the end-of-training pair values (~0.29–0.31).
  - Other descriptive readings, recorded: J1's curve is non-monotone (0.400 at t=0 dips to 0.186 at t=256 — functional convergence while both runs learn shared unigram/bigram statistics — then recovers to ~0.31); J1 top-1 disagreement is 1.0000 at t ∈ {0,4} (two near-untrained models argmax-disagree essentially everywhere — consistent with init-dominated constant-ish predictions, boundary value explained, not suspicious) settling to 0.235; ceiling d_θ is n/a by design (`key_mismatch`, cross-size).
- **Verdict (2026-07-21, final)**: **phase 1 COMPLETE — H-a PASS, H-b′ PASS, H-c REFUTED-AS-UNRUNNABLE (D-015), J1 arm valid, instrument certified.** First real-scale (160M/300B) result of the project: order-only functional divergence clears the measurement floor by 3.8× at end of training and nearly saturates joint order+init divergence from mid-training on. F-015 logged. Caveats bound to any citation: n = 1 pair per arm; eval shard is train-distribution (per registration); H-b′ is strictly weaker than the original H-b; no init-only curve exists (F-014) — the P1-vs-J1 comparison does NOT isolate the init's own contribution (J1−P1 confounds init with init×order interaction), so nothing here tests H-c's accrual-timing prediction.
- **Follow-up**: F-014 logged (the checkpoint defect + the gate-design gap). D-014 opened for the C1 disposition — **left OPEN pending principal sign-off**, since voiding a pre-registered arm and choosing a replacement control are registration-level decisions, not harness decisions. Cheapest-first next step, before any further eval spend: run `src/e009_audit_checkpoints.py` against the **J1 repos (`pythia-160m-seed1`/`-seed2`), which have never been touched** and may carry the same defect — ~2 GB of step0/1/4 downloads versus the ~25 GB of a full phase-1 relaunch. Also pending: drop `step1` from the grid; re-run the interrupted cells; run the reproducibility cell before any number is quoted. *(2026-07-21: J1 audit run — clean, see phase 0b-J1 above; step1 dropped; interrupted-cell resume and reproducibility cell wait on D-014 closure.)*
- **Amended registration (2026-07-21, per D-015 — logged and committed BEFORE the phase-1 resume runs)**:
  - **Arms**: P1 (order-only) and J1 (joint reference), both continuity-audited. **C1 cells at t ≥ 1 are removed from the job list** (arm void per F-014; recorded 2026-07-20 C1 cells stay in the JSON, cited only as void). `C1@0` = 1.247474 is retained as a standalone init-distance reference. Grid: the 12-point grid without `step1`.
  - **H-a (amended mechanically, no threshold touched)**: floor < d_f(X, 143000) for X ∈ {P1, J1}, and d_f(P1, 143000) < ceiling. The C1 clauses are void, not failed. J1-vs-ceiling remains recorded-not-gated, as originally registered.
  - **H-b as registered: REFUTED-AS-UNRUNNABLE** (no init-only control exists in this artifact set). **H-b′ (new, registered now against J1 cells that do not yet exist)**: d_f(J1, t) ≥ 0.8 · d_f(P1, t) at every matched grid checkpoint t ≥ 1000. Form and tolerance frozen verbatim from H-b with the void arm dropped; NOTE H-b′ is strictly weaker than H-b (max over one surviving arm) and this caveat travels with any reported number. P1's curve has been seen; J1's has not — the gated quantity is entirely unseen.
  - **H-c: REFUTED-AS-UNRUNNABLE** — it requires an init-divergence curve and none exists here. The K-4b/K-4d order-vs-init accrual prediction remains untested at real scale (this is an unrunnable-control outcome, not a refutation of the theory claim; no supersession of theory.md).
  - **New pre-registered void tripwire (F-014 re-check, free)**: any J1 cell with 4 ≤ t ≤ 1000 measuring d_θ < 0.5 voids the J1 arm (independent-init pairs must sit near ≈1.25 there; family-collapse would show ≈1e-2). Continuity beyond step4 was audited by inference only, so this is the measured backstop.
  - **Unchanged**: eval shard, metric definitions, floor/ceiling anchors, reproducibility cell (P1@16000 ×2, rel. 1e-6, still mandatory before any number is quoted), void rules (>2 void cells in a pair's curve → pair inconclusive).

---

## A-001 (2026-07-21) — Dual audit of the recorded ladder (code + claims), and the instrument fixes it produced

Not an experiment: a verification pass over everything already recorded, run as two independent Opus 4.8 reviews (one reading the harness code against the raw JSON, one reading every claim in findings.md/CLAUDE.md/the briefs back to its source number). Logged here because it is the certification the F-007..F-015 numbers rest on, and because it changed instrument code.

- **Claims audit — result: clean.** Every number in F-007..F-014 traces to a raw JSON cell in results/raw/. Pre-registrations are git-verified: each experiment's hypothesis commit precedes its result commit, so no gate was written or loosened after seeing data. E-008's constants are frozen at the pre-registration commit, which makes a refit rescue of the refuted edge law impossible rather than merely disallowed. Wording corrections from this pass (F-014 caveats into both professor briefs) landed in `ceda9e2`.
- **Code audit — four instrument defects found and fixed** (`c2fc78e`; **no recorded `results/raw/*.json` was touched**, and no recorded verdict changes):
  1. **`e009_divergence` resume path** targeted a `today()`-stamped filename instead of the newest existing `e009_divergence_*.json`. Any resume across a day boundary would have silently discarded the 20 completed cells and re-run them into a new file. Latent, never fired — but the 2026-07-21 resume *was* a cross-day resume, so this was found one step before it mattered.
  2. **Ceiling-cell `d_theta_rel` was fatal** on key/shape mismatch. The cross-size ceiling pair (160M vs 410M) necessarily mismatches, which would have killed the cell and with it the H-a ceiling. Now returns `None` plus a `d_theta_reason` string so the cross-size `d_f` survives.
  3. **`e009_gates` verdict semantics.** Verdicts keyed off a strict-keyset `bitwise_identical`, so a safetensors-vs-`.bin` *buffer* keyset difference could flip G-iii even when all common parameters were bitwise equal — which is exactly what it did. Now keys off `params_identical` (common-tensor equality); `max_rel_diff` → `max_abs_diff_over_norm` (tensor-norm denominator, removing ~1e10 artifacts). **Recorded-JSON note**: results/raw/e009_gates_2026-07-20.json predates the rename and keeps the old field names; its `G_iii=false` is the storage-format artifact reconciled in E-009 phase-0 G-iii above, not a refutation.
  4. **`pilot_edge2` void-cell double-count.** A single cell tripping both void tests incremented the counter twice and could have fired a spurious `>1-void INCONCLUSIVE` verdict. The recorded E-008 run is unaffected (`n_void = 0`).
- **Verdict**: recorded ladder certified; four latent instrument defects removed, three of them on paths E-009 was about to exercise. **Lesson (K-1 of instrument discipline): the defects clustered in code that had never run on the failure path** — resume, cross-size cells, mismatched keysets. Passing runs certify only the paths they took.
