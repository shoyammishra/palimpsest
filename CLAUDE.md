# Palimpsest — Optimization Path Integrability in Deep Neural Networks

## Active Context
- Status: In Progress — **M3.2 first measurement COMPLETE (E-009 / F-015, 2026-07-21)**; M3.1 pilot ladder COMPLETE (E-000..E-008, F-007..F-013). Prior rung: E-008 **REFUTED-as-stated** (F-013): the exact edge law ρ = c·K·η failed both pre-registered gates at the margin (discriminating small-η cell +25.4% vs ±25%; T=128 R² 0.9791 vs 0.98) while beating both nulls 4–5× (median err 8.8% vs 38/46%) — no refit rescue, F-012 §3 superseded. Load-bearing discovery: prefactor is strongly T-dependent (c(128)/c(64) = 0.29); post-hoc inversion-FRACTION law ρ = ĉ·(K/K_max)·η reconciles to 18% (ĉ 66 vs 78) — flagged, needs own pre-registration at third T. η-suppression slightly sub-linear (log-slope 0.86–0.94). r < 1 everywhere incl. T=128 (max 0.26). M3.2 scoping DONE (D-013): PolyPythias 160M decoupled pairs selected — P1 = (data-seed1, data-seed2) order-only (genuine sequence-level permutation, same init), C1 = (weight-seed1, weight-seed2) init-only control, J1 = (seed1, seed2) joint; 154 ckpts/run as git branches; honest limits logged (batch-composition ≠ batch-order at sequence granularity; K ~ 5×10¹⁵ ⇒ divergence/recoverability measurements, not full transport). Prior rungs: E-007/F-012 (frontier not wall), E-006/F-011 (sampling closed; α ≈ 0.19), E-005/F-010 (O(K+T) chain), E-004/F-009 (Adam Θ(η)), E-003/F-008 (first transport), E-000..002/F-007.
- Current task: **E-009 COMPLETE (2026-07-21) — F-015 logged, first real-scale result.** Phase 1 finished under the D-015-amended registration (36 cells, zero void): **H-a PASS** (P1@143000 = 0.290 sym-KL, 3.81× floor, below 0.460 ceiling), **H-b′ PASS** (J1/P1 ≥ 1.007 at all t ≥ 1000), repro cell exact (rel 0.0, deterministic — designed boundary), J1 tripwire clean (d_θ ≥ 1.24 everywhere). **Headline (F-015): order-only fraction of joint divergence rises monotonically ≈0 → 0.95** (0.319@256, 0.546@1000, 0.857@4000, 0.932@16000, ~0.95 late) — by mid-training, order alone nearly saturates order+init functional divergence, while d_θ stays on independent scales (P1 0.954 vs J1 1.408); the init's functional imprint washes out, order's persists. Caveats bound to any citation: n=1 pair/arm, train-distribution eval shard, H-b′ weaker than H-b, floor strongly t-dependent (0.680 at 1000→2000 exceeds every pair value ⇒ separation-of-scales is late-training only; t ≲ 1000 cells support no pair claims), H-c untested (no init-only curve exists publicly — F-014/D-015; J1−P1 confounds init with init×order). Background: D-014 closed by D-015 ((a)+(c): single-arm order-only; C1@0 = 1.2475 kept as init-distance reference; H-b/H-c REFUTED-AS-UNRUNNABLE); J1 audited clean (`--set=j1`, cont 5.6e-5; seed repos' `step1` ≡ `step0` init-copy quirk); Opus survey: no public downloadable init-only pair (closest: AllenAI signal-and-noise OLMo-2 1B, weights internal); F-014 cause best supported = ws runs launched from shared init, seed still driving the shuffle. **Upstream report FILED 2026-07-21: https://github.com/EleutherAI/pythia/issues/203** (F-014 defect, posted as `shoyammishra`; cites related open issue #198 "Weight inconsistencies" as prior art and confirms its adjacent case — our `step1` ≡ `step0` observation in seed{1,2} — while framing the `weight-seed` non-descent as the separate, more consequential claim). **User actions pending: (1) send docs/allenai_email_draft.md to `davidh@allenai.org` — D-015 option-(b) ask for the Signal-and-Noise 1B init-seed models, drafted 2026-07-21, no mail capability in-harness so the user sends; availability re-checked that day (paper claims release, only dataset+code are public). (2) HF cross-link discussion on `pythia-160m-weight-seed1` — deprioritized, text staged in docs/upstream_issue_draft.md, blocked on invalid local HF token (`hf auth login --force`).** Next candidates (pick via decision entry): update professor briefs with F-015; M3.3/M3.4 scoping (recoverability probes on P1); bonus order-only pairs (main, ds1)/(main, ds2) after a continuity gate on the main run; M5 items. **2026-07-21 dual audit (code + claims, two Opus reviews) DONE**: all recorded findings F-007..F-014 verified clean (every number traces to raw JSON; pre-registration git-verified; no refit rescue possible — E-008 constants frozen at the pre-registration commit). Instrument fixes applied same day: (1) e009_divergence resume now targets the newest existing `e009_divergence_*.json` instead of a today()-stamped path (the old code would have silently discarded the 20 completed cells on any cross-day resume); (2) ceiling-cell `d_theta_rel` made non-fatal (returns None + `d_theta_reason` on key/shape mismatch so the cross-size `d_f` survives); (3) e009_gates verdicts now key off `params_identical` (common-tensor equality; old `bitwise_identical`/`max_rel_diff` renamed → `params_identical`/`max_abs_diff_over_norm` — recorded 2026-07-20 JSON keeps old field names and its `G_iii=false` is a storage-format artifact, reconciled in experiment_log E-009); (4) pilot_edge2 void CELLS now counted once (was double-counted when one cell tripped both void tests; recorded run unaffected, n_void=0). M2 leftovers deferred to M5 (K-3 constants, K-5b proof, Adam constant-tracking, structured estimators per F-011; + K-1 sequence-granularity remark per D-013).
- Key files: src/pilot_defects.py, src/pilot_transport.py, src/pilot_adam.py, src/pilot_transport_batched.py, src/pilot_sampling.py, src/pilot_edge.py, src/pilot_edge2.py, src/e009_gates.py, src/e009_divergence.py (C1 dropped, J1 tripwire, step1-less grid), src/e009_audit_checkpoints.py (phase-0b continuity gates G-v/G-vi; `--set=j1` mode), docs/experiment_log.md (E-000..E-009 + amended E-009 registration), docs/theory.md (K-1..K-5, P-1), docs/design.md (v0.4), docs/decision_log.md (D-010..D-015; D-014 closed by D-015), docs/findings.md (F-001..F-015), docs/upstream_issue_draft.md (EleutherAI report — FILED as pythia#203; HF cross-post text still staged/unsent), docs/allenai_email_draft.md (D-015 option-(b) ask to Ai2, drafted NOT sent — user action), docs/professor_brief.md (external-facing brief, revised 2026-07-21 with F-014 caveats; citations #3/#4/#6 title-verified via arXiv abs pages), docs/professor_brief.html (typeset lay-reader rendering, same 2026-07-21 revision — keep in sync with the .md)
- Open questions: Why does the order-only fraction saturate at ~0.95 rather than 1.0 — is the residual ~5% the init's durable functional imprint, interaction, or single-pair noise (needs more seed pairs or the AllenAI weights)? Does AllenAI release the signal-and-noise 1B seed weights (only known path to a real init-only control; else option (d) at M5)? Why do ws1/ws2 diverge *less* than the ds pair (1.38e-2 vs 2.52e-2 at @256) — unexplained even under the shared-init-launch hypothesis? What does J1's non-monotone dip (0.400→0.186@256→0.31) say about shared-statistics learning vs seed memory? Does the inversion-fraction law ρ = ĉ·(K/K_max)·η^p survive pre-registration at a third T (and what is p, given sub-linear η drift)? What inversion-set geometry sets the family prefactor (Π_⊥ hook, M5)? Can structured estimators beat enumeration where unbiased sampling failed (F-011)? Does α ≈ 0.19 hold at larger T? Homotopy generalization of K-1 to non-permutation pairs (needed for H4)?

## Conventions
- Read-first order: CLAUDE.md → docs/roadmap.md → docs/design.md → whatever Active Context points at.
- docs/research_spec.md is the verbatim source-of-truth spec. Never edit it; supersede via decision_log entries.
- Hard gate: no experiment, no compute spend, before the M1 novelty verdict is logged in docs/findings.md.
- Every experiment gets a hypothesis logged in docs/experiment_log.md BEFORE it runs. One variable at a time.
- Negative/impossibility results are first-class outcomes — the project does not depend on a repair operator existing.
- Delegation: Opus 4.8 subagents for deep research/reasoning; agents return reports, principal engineer reviews and writes all files.
- Decisions go in docs/decision_log.md (options, choice, why, how to reverse). If it's not written down, it doesn't exist.
- Small commits, often. `git diff --stat` before committing. Never leave the repo broken.
- Follow FABLE5_PROJECT_PROMPT.md for the full workflow (checkpoint protocol, loop, handoff template).
