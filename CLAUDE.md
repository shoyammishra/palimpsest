# Palimpsest — Optimization Path Integrability in Deep Neural Networks

## Active Context
- Status: In Progress — M3.1 pilot ladder COMPLETE (E-000..E-008, F-007..F-013). Latest: E-008 **REFUTED-as-stated** (F-013): the exact edge law ρ = c·K·η failed both pre-registered gates at the margin (discriminating small-η cell +25.4% vs ±25%; T=128 R² 0.9791 vs 0.98) while beating both nulls 4–5× (median err 8.8% vs 38/46%) — no refit rescue, F-012 §3 superseded. Load-bearing discovery: prefactor is strongly T-dependent (c(128)/c(64) = 0.29); post-hoc inversion-FRACTION law ρ = ĉ·(K/K_max)·η reconciles to 18% (ĉ 66 vs 78) — flagged, needs own pre-registration at third T. η-suppression slightly sub-linear (log-slope 0.86–0.94). r < 1 everywhere incl. T=128 (max 0.26). M3.2 scoping DONE (D-013): PolyPythias 160M decoupled pairs selected — P1 = (data-seed1, data-seed2) order-only (genuine sequence-level permutation, same init), C1 = (weight-seed1, weight-seed2) init-only control, J1 = (seed1, seed2) joint; 154 ckpts/run as git branches; honest limits logged (batch-composition ≠ batch-order at sequence granularity; K ~ 5×10¹⁵ ⇒ divergence/recoverability measurements, not full transport). Prior rungs: E-007/F-012 (frontier not wall), E-006/F-011 (sampling closed; α ≈ 0.19), E-005/F-010 (O(K+T) chain), E-004/F-009 (Adam Θ(η)), E-003/F-008 (first transport), E-000..002/F-007.
- Current task: **E-009 BLOCKED on D-014 (open).** Phase 0 gates passed, phase 1 launched and was interrupted (Ctrl-C/hard kill during a `weight-seed2@step512` download — resumable, 0-byte `.incomplete` blob, no `void` record because `KeyboardInterrupt` bypasses the per-cell `except Exception`). Phase 0b audit then found **F-014**: the `weight-seed{1,2}` checkpoints do NOT continue from their own `step0` inits (continuity ratio = 1.000 at t = 1..256; all four decoupled repos numerically identical at `step1`; C1 d_θ 1.2475@0 → 4.69e-5@4, impossible for independent inits). **C1 is void beyond step0 ⇒ H-b and H-c have no control arm.** Metric instrument independently exonerated (reproduces recorded JSON bit-for-bit). P1 usable from `@4` on, ungated. Next, cheapest-first, BEFORE any further eval spend: run `src/e009_audit_checkpoints.py` on the untested **J1** repos (~2 GB read-only vs ~25 GB relaunch), then close D-014 (options a–d) — the C1 replacement must be pre-registered, not chosen against the P1 curve already seen. Also pending: drop `step1` from the grid; resume interrupted cells; run the never-executed reproducibility cell (P1@16000 ×2, rel 1e-6) before quoting any number. M2 leftovers deferred to M5 (K-3 constants, K-5b proof, Adam constant-tracking, structured estimators per F-011; + K-1 sequence-granularity remark per D-013).
- Key files: src/pilot_defects.py, src/pilot_transport.py, src/pilot_adam.py, src/pilot_transport_batched.py, src/pilot_sampling.py, src/pilot_edge.py, src/pilot_edge2.py, src/e009_gates.py, src/e009_divergence.py, src/e009_audit_checkpoints.py (phase-0b continuity gates G-v/G-vi), docs/experiment_log.md (E-000..E-009), docs/theory.md (K-1..K-5, P-1), docs/design.md (v0.4), docs/decision_log.md (D-010..D-014; **D-014 OPEN**), docs/findings.md (F-001..F-014), docs/professor_brief.md (external-facing brief, refreshed 2026-07-20 with the E-008 refutation; citations #3/#4/#6 title-verified via arXiv abs pages — **not yet updated for F-014**), docs/professor_brief.html (typeset lay-reader rendering of the same brief; same E-008-era content, **also not yet updated for F-014** — keep in sync with the .md)
- Open questions: **D-014 — what replaces the void init-only control (or does E-009 go single-arm)?** Does J1 carry the same checkpoint defect? What is the upstream cause of F-014 (step0 upload, step≥1 upload, or run provenance) and should it be raised with EleutherAI? Why do ws1/ws2 diverge at all from `step1` given a shared init and supposedly fixed data order — and by *less* than the ds pair? Does the inversion-fraction law ρ = ĉ·(K/K_max)·η^p survive pre-registration at a third T (and what is p, given sub-linear η drift)? What inversion-set geometry sets the family prefactor (Π_⊥ hook, M5)? Can structured estimators beat enumeration where unbiased sampling failed (F-011)? Does α ≈ 0.19 hold at larger T? Homotopy generalization of K-1 to non-permutation pairs (needed for H4)?

## Conventions
- Read-first order: CLAUDE.md → docs/roadmap.md → docs/design.md → whatever Active Context points at.
- docs/research_spec.md is the verbatim source-of-truth spec. Never edit it; supersede via decision_log entries.
- Hard gate: no experiment, no compute spend, before the M1 novelty verdict is logged in docs/findings.md.
- Every experiment gets a hypothesis logged in docs/experiment_log.md BEFORE it runs. One variable at a time.
- Negative/impossibility results are first-class outcomes — the project does not depend on a repair operator existing.
- Delegation: Opus 4.8 subagents for deep research/reasoning; agents return reports, principal engineer reviews and writes all files.
- Decisions go in docs/decision_log.md (options, choice, why, how to reverse). If it's not written down, it doesn't exist.
- Small commits, often. `git diff --stat` before committing. Never leave the repo broken.
- Follow FABLE5_PROJECT_PROMPT.md for the full workflow (checkpoint protocol, loop, handoff template).
