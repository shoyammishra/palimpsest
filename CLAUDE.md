# Palimpsest — Optimization Path Integrability in Deep Neural Networks

## Active Context
- Status: In Progress. **M3.2 first measurement COMPLETE** (E-009 / F-015, 2026-07-21) — first real-scale result. M3.1 pilot ladder COMPLETE (E-000..E-008 → F-007..F-013); recorded ladder certified by the 2026-07-21 dual audit (experiment_log A-001). Ladder detail lives in docs/experiment_log.md and docs/findings.md — do not re-summarize it here.
- Headline (F-015, cite with its caveats or not at all): at Pythia-160M/300B, **order-only functional divergence clears the measurement floor 3.8× and reaches ~95% of joint order+init divergence by late training** — the init's functional imprint washes out, order's persists. Caveats that must travel with the number: n=1 pair per arm; train-distribution eval shard; H-b′ is weaker than the registered H-b; the floor is strongly t-dependent, so separation-of-scales is a **late-training** claim only (t ≲ 1000 supports no pair claims); H-c untested — no init-only control exists publicly (F-014 killed the candidate), so J1−P1 confounds init with init×order. Full numbers: findings.md F-015, experiment_log E-009 phase-1 resume.
- Current task: **next rung still unpicked; candidate (1) is DONE.** 2026-07-22: F-015 folded into docs/professor_brief.{md,html} as a new §4.3 "Measured at real scale" (cost caveat renumbered 4.3→4.4), with two new §6 risk entries (n=1 per arm; no init-only control) and Frankle et al. 2020 ×2 added as preemptively-cited prior art. **Three self-corrections logged in the same change** (all found by re-deriving from results/raw/e009_divergence_2026-07-20.json, none gate-bearing): F-015 §3's "rises monotonically" is false at the last grid point (0.9934@128000 → 0.9489@143000; monotone at 10 of 12); F-015 §6's "~9%" P1 relaxation is 8.4%; E-009's "d_θ rises monotonically" is false (P1 d_θ_rel peaks 0.965503@16000, eases to 0.953915). Remaining candidates, to be chosen via a decision entry: (2) M3.3/M3.4 scoping — recoverability probes on P1; (3) bonus order-only pairs (main, ds1)/(main, ds2), gated on a continuity audit of the main run; (4) M5 items. M2 leftovers deferred to M5: K-3 constants, K-5b proof, Adam constant-tracking, structured estimators (F-011), K-1 sequence-granularity remark (D-013).
- Outward-facing state: **F-014 filed upstream** as https://github.com/EleutherAI/pythia/issues/203 (2026-07-21, as `shoyammishra`). **Ai2 request SENT 2026-07-21** to `davidh@allenai.org` (D-015 option-(b), text in docs/allenai_email_draft.md) — awaiting reply; if weights arrive, continuity-audit first, then pre-register against unseen curves. Still pending (deprioritized): HF cross-link post on `pythia-160m-weight-seed1`, text staged in docs/upstream_issue_draft.md, blocked on an invalid local HF token (`hf auth login --force`).
- Key files: src/pilot_defects.py, src/pilot_transport.py, src/pilot_adam.py, src/pilot_transport_batched.py, src/pilot_sampling.py, src/pilot_edge.py, src/pilot_edge2.py, src/e009_gates.py, src/e009_divergence.py (C1 dropped, J1 tripwire, step1-less grid), src/e009_audit_checkpoints.py (phase-0b continuity gates G-v/G-vi; `--set=j1` mode), docs/experiment_log.md (E-000..E-009 + amended E-009 registration), docs/theory.md (K-1..K-5, P-1), docs/design.md (v0.4), docs/decision_log.md (D-010..D-015; D-014 closed by D-015), docs/findings.md (F-001..F-015), docs/upstream_issue_draft.md (EleutherAI report — FILED as pythia#203; HF cross-post text still staged/unsent), docs/allenai_email_draft.md (D-015 option-(b) ask to Ai2 — SENT 2026-07-21, awaiting reply), docs/professor_brief.md (external-facing brief, revised 2026-07-22 with F-015 §4.3; citations #3/#4/#6 title-verified via arXiv abs pages), docs/professor_brief.html (typeset lay-reader rendering, same 2026-07-22 revision — keep in sync with the .md)
- Open questions: Why does the order-only fraction saturate at ~0.95 rather than 1.0 — is the residual ~5% the init's durable functional imprint, interaction, or single-pair noise (needs more seed pairs or the AllenAI weights)? Does AllenAI release the signal-and-noise 1B seed weights (only known path to a real init-only control; else option (d) at M5)? Why do ws1/ws2 diverge *less* than the ds pair (1.38e-2 vs 2.52e-2 at @256) — unexplained even under the shared-init-launch hypothesis? What does J1's non-monotone dip (0.400→0.186@256→0.31) say about shared-statistics learning vs seed memory? Does the inversion-fraction law ρ = ĉ·(K/K_max)·η^p survive pre-registration at a third T (and what is p, given sub-linear η drift)? What inversion-set geometry sets the family prefactor (Π_⊥ hook, M5)? Can structured estimators beat enumeration where unbiased sampling failed (F-011)? Does α ≈ 0.19 hold at larger T? Homotopy generalization of K-1 to non-permutation pairs (needed for H4)?

## Conventions
- Read-first order: CLAUDE.md → docs/roadmap.md → docs/design.md → whatever Active Context points at.
- docs/research_spec.md is the verbatim source-of-truth spec. Never edit it; supersede via decision_log entries.
- Hard gate: no experiment, no compute spend, before the M1 novelty verdict is logged in docs/findings.md.
- Every experiment gets a hypothesis logged in docs/experiment_log.md BEFORE it runs. One variable at a time.
- Negative/impossibility results are first-class outcomes — the project does not depend on a repair operator existing.
- Delegation: Opus 4.8 subagents for deep research/reasoning; agents return reports, principal engineer reviews and writes all files.
- Decisions go in docs/decision_log.md (options, choice, why, how to reverse). If it's not written down, it doesn't exist.
- Small commits, often. `git diff --stat` before committing. Never leave the repo broken.
- Follow FABLE5_PROJECT_PROMPT.md for the full workflow (checkpoint protocol, loop, handoff template).
