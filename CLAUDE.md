# Palimpsest — Optimization Path Integrability in Deep Neural Networks

## Active Context
- Status: In Progress. **M3.2 first measurement COMPLETE** (E-009 / F-015, 2026-07-21) — first real-scale result. M3.1 pilot ladder COMPLETE (E-000..E-008 → F-007..F-013); recorded ladder certified by the 2026-07-21 dual audit (experiment_log A-001). Ladder detail lives in docs/experiment_log.md and docs/findings.md — do not re-summarize it here.
- Headline (F-015, cite with its caveats or not at all): at Pythia-160M/300B, **order-only functional divergence clears the measurement floor 3.8× and reaches ~95% of joint order+init divergence by late training** — the init's functional imprint washes out, order's persists. Caveats that must travel with the number: n=1 pair per arm; train-distribution eval shard; H-b′ is weaker than the registered H-b; the floor is strongly t-dependent, so separation-of-scales is a **late-training** claim only (t ≲ 1000 supports no pair claims); H-c untested — no init-only control exists publicly (F-014 killed the candidate), so J1−P1 confounds init with init×order. Full numbers: findings.md F-015, experiment_log E-009 phase-1 resume.
- Current task: **D-018 DECIDED + E-012 (R-3) REGISTERED — NOT RUN (2026-07-22). Next task (separate future session): run E-012 Phase B.** D-018 closes the E-011b fork: R-3 (the Σ_E functional-projection readout) is chosen over E-011c graft-salvage — both post-E-011b signals converge on it (F-017 needs the functional weighting R-1/R-2 avoided; F-018 killed cross-model mixing, which R-3 does not do), and R-3 alone instruments a *proved* theorem (P-1). E-011c is PARKED as the D-018 reversal path. E-012 is registered two-phase: **Phase 0** = settle+certify the Σ_E (empirical-Fisher) estimator via a frozen instrument check I-Σ (the D-017 precondition, made a hard gate; ≥3 small-perturbation δ must reproduce measured sym-KL to rel ≤0.25, else E-012 halts at Phase 0 — no tolerance loosening); **Phase 1** (only if I-Σ passes) = (R3a) recoverable-fraction-vs-access-class ladder frac_rec(𝓘) = 1 − dist_{Σ_E}(Δθ_T,S)²/‖Δθ_T‖²_{Σ_E} (P-1/Π_⊥, H5/path-burned; gated (G) vs a rank-matched random-subspace floor) + (R3b) Σ_E-weighted directional lock-in t\*_func vs F-017's raw t\*=128000 (H2, the F-017 bridge). 0 new downloads; introduces backward/gradients for the first time (Fisher estimator only, `d_f` untouched). Prior rung: **E-011b (R-2) COMPLETE — F-018 logged (2026-07-22). Instrument certified 5/5 (I-3 reproduced F-015's d_f to rel 1.4e-10); subject verdict INCONCLUSIVE-BY-FROZEN-RULE with a first-class structural negative: single-block α-interpolation grafts BREAK the model on essentially every function-bearing block (7 of 8 hard swaps void; attn_qkv — 87% of Δ² — void at every α; CE band 3.0016 fired 23/35 cells; frozen abort skipped RF/T2/SYM, no rescue). Only ln is fully graft-tolerant (α=1 valid-but-degraded, and far from BOTH parents — off-manifold, not transport); embed_in/final_ln valid to α=0.5. H5 UNTESTED, not refuted.** *(Fork now CLOSED by D-018 — R-3 chosen, E-012 registered above; E-011c parked.)* Prior rung: E-011a (R-1) COMPLETE — F-017. R-1 temporal localization ran analysis-only (`src/e011a_direction.py`, raw e011a_direction_2026-07-22.json; **0 new downloads**, forward-free, no measured-number code path touched). **All five frozen gates PASS** (I-1 repro rel 0.0; I-2 self/neg/random 1.0/−1.0/1.36e-4; I-3 d_θ matches F-015 raw to ~1e-16; F-1; F-2). **Headline (F-017, cite with caveats): a D-006 dissociation** — the *raw parameter-space* endpoint order-direction is set **LATE** (cos(Δθ_16000,Δθ_T)=0.294, t\* for cos≥0.9 = 128000) while F-015's *functional* fraction is already **0.93 at t=16000**; the two do not contradict (parameter direction ≠ function). F-2 PASS = above-floor persistence exists by mid-training (C_drift=0.0048 ≈0; cos 0.294 ≫ 0.105) but "present," NOT "locked" (late-t→1 is partly trivial trajectory-continuity). (W) per-block: the low mid-training cosine is **network-wide, attn_qkv-dominated (0.46 cos, 62% share)**, NOT a high-norm-block artifact — so R-1 refutes the naive "artifact" reading but **cannot confirm functional recoverability** (still parameter-space; Σ_E is R-3). **H2's early-lock-in, in its raw reading, descriptively did NOT appear at real scale** — a descriptive surprise (nothing gated on it; fp16 + D-006 mean the *functional* H2 is untested), feeding the R-2/R-3 weighting design. P2/P3 reproduce the dissociation qualitatively at n=3-correlated; mid-training magnitude is pair-dependent (0.29/0.44/0.47, F-016 caveat). **Next**: E-011b (R-2 spatial localization) needs its own registration incl. the frozen graft-validity CE-loss band (D-017; NOT this session). **Deferred**: (4) M5 items (K-3 constants, K-5b proof, Adam constant-tracking, structured estimators (F-011), K-1 sequence-granularity remark (D-013)); professor_brief refresh to fold F-016 n=3 into §4.3 + note the R-1 dissociation.
- Outward-facing state: **F-014 filed upstream** as https://github.com/EleutherAI/pythia/issues/203 (2026-07-21, as `shoyammishra`). **Ai2 request SENT 2026-07-21** to `davidh@allenai.org` (D-015 option-(b), text in docs/allenai_email_draft.md) — awaiting reply; if weights arrive, continuity-audit first, then pre-register against unseen curves. Still pending (deprioritized): HF cross-link post on `pythia-160m-weight-seed1`, text staged in docs/upstream_issue_draft.md, blocked on an invalid local HF token (`hf auth login --force`).
- Key files: src/pilot_defects.py, src/pilot_transport.py, src/pilot_adam.py, src/pilot_transport_batched.py, src/pilot_sampling.py, src/pilot_edge.py, src/pilot_edge2.py, src/e009_gates.py, src/e009_divergence.py (C1 dropped, J1 tripwire, step1-less grid), src/e009_audit_checkpoints.py (phase-0b continuity gates G-v/G-vi; `--set=j1`, `--set=main` modes; per-set GVI_SKIP_REVS), src/e010_divergence.py (E-010 bonus order-only pairs P2/P3; imports the E-009 measured-number funcs unchanged; own raw file + mirrored d_θ>0.5 tripwire), src/e011a_direction.py (R-1 directional-persistence analysis; READ-ONLY, forward-free; mirrors the certified .bin load path + from_pretrained for main; own raw file), src/e011b_graft.py (R-2 graft harness; certified d_f by import, frozen CE band + abort rules; results/raw/e011b_graft_2026-07-22.json, run commit b0ca684), docs/experiment_log.md (E-000..E-011b COMPLETE; amended E-009 + complete E-010 + E-011a/E-011b registrations+results; E-012 REGISTERED — NOT RUN, R-3 recoverability ladder + Σ_E lock-in, module src/e012_recoverability.py pending Phase B), docs/theory.md (K-1..K-5, P-1), docs/design.md (v0.4), docs/recoverability_scoping.md (M3.4 probe scoping sketch; its §7 draft D-017 skeleton now adopted as decision_log D-017), docs/decision_log.md (D-010..D-018; D-014 closed by D-015; D-016 picks E-010; D-017 adopts R-1→R-2 program, registers R-1; D-018 closes the E-011b fork — R-3 chosen over E-011c, E-012 registered, E-011c parked), docs/findings.md (F-001..F-018; F-017 = R-1 dissociation, F-018 = R-2 graft collapse), results/raw/e011a_direction_2026-07-22.json (R-1 raw; run commit 0d6fd4b), docs/upstream_issue_draft.md (EleutherAI report — FILED as pythia#203; HF cross-post text still staged/unsent), docs/allenai_email_draft.md (D-015 option-(b) ask to Ai2 — SENT 2026-07-21, awaiting reply), docs/professor_brief.md (external-facing brief, revised 2026-07-22 with F-015 §4.3; citations #3/#4/#6 title-verified via arXiv abs pages), docs/professor_brief.html (typeset lay-reader rendering, same 2026-07-22 revision — keep in sync with the .md)
- Open questions: **(F-018, being addressed) Is the order trace spatially readable by ANY graft-class operator, or only via projection readouts?** Naive whole-type-block linear grafts are outside their validity region almost everywhere (F-018). **→ D-018 pursues the projection route: R-3's Σ_E functional-projection readout, registered as E-012 (frac_rec ladder + (G) random-subspace floor) — REGISTERED, NOT RUN.** The complementary E-011c graft-window question (does permutation/re-basin alignment, much smaller α, or single-layer blocks open a usable graft window?) is **PARKED as the D-018 reversal path**, resurrected only under the D-018 reversal conditions. **(F-017, being addressed by E-012 R3b) Does a functional/Σ_E-weighted direction lock in EARLIER than the raw one?** R-1 found a D-006 dissociation — raw parameter-direction sets late (cos 0.29 @16000) while F-015's functional divergence saturates early (0.93 @16000); whether the *functional* version of H2's early-lock-in holds is **now E-012's (R3b) t\*_func readout (REGISTERED, NOT RUN)** — its Σ_E instrument (I-Σ) must certify first. Why is the raw mid-training cosine pair-dependent (P1 0.29 vs P2/P3 0.44/0.47) — correlated-pairs noise or a real main-vs-dsN asymmetry (cf. F-016's d_θ>1.0 for main-pairs)? Why does the order-only fraction saturate at ~0.95 rather than 1.0 — is the residual ~5% the init's durable functional imprint or an init×order interaction? (**E-010/F-016 narrowed this: single-pair noise is DISFAVORED — three order-only pairs agree to ~1% — so the residual is structural; splitting durable-imprint from interaction still needs an init-only control, i.e. the AllenAI weights.**) Does AllenAI release the signal-and-noise 1B seed weights (only known path to a real init-only control; else option (d) at M5)? Why do ws1/ws2 diverge *less* than the ds pair (1.38e-2 vs 2.52e-2 at @256) — unexplained even under the shared-init-launch hypothesis? What does J1's non-monotone dip (0.400→0.186@256→0.31) say about shared-statistics learning vs seed memory? Does the inversion-fraction law ρ = ĉ·(K/K_max)·η^p survive pre-registration at a third T (and what is p, given sub-linear η drift)? What inversion-set geometry sets the family prefactor (Π_⊥ hook, M5)? Can structured estimators beat enumeration where unbiased sampling failed (F-011)? Does α ≈ 0.19 hold at larger T? Homotopy generalization of K-1 to non-permutation pairs (needed for H4)?

## Conventions
- Read-first order: CLAUDE.md → docs/roadmap.md → docs/design.md → whatever Active Context points at.
- docs/research_spec.md is the verbatim source-of-truth spec. Never edit it; supersede via decision_log entries.
- Hard gate: no experiment, no compute spend, before the M1 novelty verdict is logged in docs/findings.md.
- Every experiment gets a hypothesis logged in docs/experiment_log.md BEFORE it runs. One variable at a time.
- Negative/impossibility results are first-class outcomes — the project does not depend on a repair operator existing.
- Delegation: Opus 4.8 subagents for deep research/reasoning; agents return reports, principal engineer reviews and writes all files.
- Decisions go in docs/decision_log.md (options, choice, why, how to reverse). If it's not written down, it doesn't exist.
- Small commits, often. `git diff --stat` before committing. Never leave the repo broken.
- Follow FABLE5_PROJECT_PROMPT.md for the full workflow (checkpoint protocol, loop, handoff template).
